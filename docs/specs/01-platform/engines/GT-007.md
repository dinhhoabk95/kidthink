---
spec: ENGINE-GT007
title: Tách gộp số — number-bond
area: platform
status: approved
mvp: true
phase: P4
reviewed: 2026-08-30
engine: number-bond
batch: montessori
owns:
  - Hợp đồng nội dung của engine GT-007
  - Hợp đồng vẽ của engine GT-007
  - Ma trận seed mục tiêu của engine GT-007
depends_on:
  - GAME-TEMPLATE-CONTRACT
  - GAME-LAYOUT-ENGINE
  - ENGINE-RENDER-CONTRACT
  - ENGINE-SPEC-SHEET
---

# GT-007 — Tách gộp số

## 1. Objective

Trẻ nhìn một tổng và tìm các phần cộng lại thành nó: 5 là 2 với 3, hoặc 1 với 4. Đây là mô
hình trực quan của phép cộng và phép trừ **trước khi** trẻ gặp ký hiệu. Nguồn Montessori của
nó là bộ thanh số và bộ hạt màu.

Khác `GT-001` ở chỗ đáp án không đơn nhất: một tổng có nhiều cách tách, và engine phải chấp
nhận mọi cách đúng.

## 2. Actors

| Actor | Quyền cần | Làm được gì ở đây |
|---|---|---|
| Trẻ 3–6 | — | Tương tác với engine number-bond |
| Người soạn nội dung | `content_reviewer` | Soạn `content_pack` và `difficulty_params` |
| Cổng | — | Kiểm tra tính hợp lệ |

## 3. Entry points

| Route / màn hình | Actor | Ghi chú |
|---|---|---|
| `packages/game-engine/src/templates/GT-007/template.ts` | Dev | Định nghĩa contract |
| `packages/game-engine/src/templates/GT-007/session.ts` | Dev | Logic + render |
| `docs/specs/01-platform/engines/GT-007.md` | Dev | Spec này |

## 4. Main flow

Bảy nhịp của [`engine-turn-script.md`](../engine-turn-script.md).

1. **`N1` Mở màn** — vẽ `whole` (tổng) ở trên, rồi hai khay `parts` rỗng ở dưới, rồi
   `options` ở khay chọn. Tổng luôn hiện thường trực: trẻ không phải nhớ nó.
2. **`N2` Ra đề** — `prompt` đọc tự động một lần ("năm quả chia vào hai rổ"). Kênh hình song
   song: tổng vẽ bằng vật đếm được, không chỉ bằng chữ số.
3. **`N3` Thao tác** — `drop` vật từ tổng xuống một trong hai khay phần; lối thay thế `tap`
   vật rồi `tap` khay. Trẻ chuyển qua lại giữa hai khay tuỳ ý.
4. **`N4` Phản hồi tức thì** — vật rời tổng thì tổng **không** đổi số hiện, chỉ mờ vật đã lấy —
   đó là cách bài giữ ý niệm bảo toàn. Khay phần đếm lên ngay khi vật rơi vào.
5. **`N5` Trợ giúp** — quá `hint_after_ms`: `L1` khay còn thiếu sáng viền · `L2` bàn tay ma
   mang một vật xuống khay đó · `L3` cùng động tác, chậm 0,5×, lặp.
6. **`N6` Kết lượt** — hai khay cộng lại bằng `whole` và cách tách nằm trong `parts` hợp lệ →
   `checkWinCondition()` trả `true`. Cách tách của trẻ ở lại một nhịp kèm câu đọc lại "năm là
   hai với ba".
7. **`N7` Chơi lại** — seed mới đổi vị trí vật và thứ tự `options`; `whole` và `part_count`
   giữ nguyên; hai khay rỗng lại. Trẻ thường chọn **cách tách khác** lần này — engine chấp
   nhận mọi cách đúng nên đó là biến thể có sẵn, không phải bài mới.

## 5. Alternative flows

Tám nhánh bắt buộc (`BR-ESS-19`), cộng nhánh riêng của engine.

| # | Nhánh | Điều kiện | Hành vi |
|---|---|---|---|
| 1 | Thao tác sai, còn lượt | Tổng hai khay khác `whole`, `allow_retry` bật | Nhịp hổ phách trên tổng; vật trong khay **giữ nguyên** để trẻ thêm bớt |
| 2 | Thao tác sai, hết lượt | `allow_retry` tắt | Lượt đóng; một cách tách đúng hiện ra một nhịp bên cạnh cách của trẻ |
| 3 | Trẻ dừng lại | Quá `hint_after_ms` | Leo thang `L1`→`L3` như `N5` |
| 4 | Không nghe được lời đọc | Tắt âm hoặc thiếu giọng | Tổng vẽ bằng vật đếm được là kênh đề; phát `tts_unavailable` |
| 5 | Asset hỏng | Ref không phân giải | Ô giữ chỗ; số lượng vật cấm — NEVER đổi, vì lượng là nội dung |
| 6 | Thiết bị yếu · `prefers-reduced-motion` | FPS tụt | Bỏ hạt lớp 4; giữ hiệu ứng vật rơi vào khay |
| 7 | Bỏ dở giữa lượt | Rời màn khi tách dở | Phiên `abandoned`; quay lại là lượt mới |
| 8 | Chơi lại lần thứ n | Trẻ bấm chơi lại | Bàn mới, `whole` giữ nguyên; cách tách khác vẫn thắng |

## 6. Business rules

| ID | Rule | Vì sao |
|---|---|---|
| `BR-E007-01` | Band hợp lệ: `3-4`, `4-5`, `5-6` | Phù hợp phát triển vận động và nhận thức của lứa tuổi |
| `BR-E007-02` | `checkWinCondition()` thuần — không side effect | Tính toán không được ảnh hưởng trạng thái |

## 7. Data

Contract đầy đủ nằm ở schema trong `template.ts` — cấm chép lại schema (`BR-ESS-03`).

## 8. API contract

Không có. Engine chạy trong tiến trình frontend/canvas runtime.

## 9. Acceptance criteria

```gherkin
Scenario: GT-007 — mọi cách tách đúng đều được chấp nhận
  Given whole là 5 và options chứa cả cặp 2-3 lẫn cặp 1-4
  When trẻ chọn cặp 1-4
  Then kết quả là đạt

Scenario: GT-007 — tổng của các phần phải bằng whole
  Given một content_pack có whole là 5 và parts cộng lại bằng 6
  When server parse content_pack
  Then trả 422 CONTENT_PACK_INVALID

Scenario: BR-E007-01 — band hợp lệ
  Given một level GT-007
  When band nằm ngoài phạm vi cho phép
  Then cổng validate đỏ

Scenario: BR-E007-02 — checkWinCondition thuần
  Given một phiên GT-007
  When gọi checkWinCondition 100 lần
  Then trạng thái không đổi
```

## 10. Boundaries

**Always** — phạm vi số theo band: ≤5 ở `3-4`, ≤10 ở `4-5`, ≤20 ở `5-6`; chấp nhận mọi cách tách đúng.

**Ask first** — `part_count` 3 ở band 4-5; dùng `number-bond-tree` dưới band 5-6.

**Never** — tổng vượt phạm vi số của band; chỉ chấm một cách tách khi có nhiều cách đúng.

## 11. Open questions

| # | Câu hỏi | Chặn gì | Chặn phase | Chủ |
|---|---|---|---|---|
| 1 | Engine đang có 1 chủ đề trên 6 level. Lô nội dung tiếp theo lấp `theme_span` trước hay `thinking_span` trước? | Bậc 2 của engine này | P5 | Nội dung |

## 12. Hợp đồng vẽ

Hợp đồng chung, gồm bốn lớp và năm trạng thái thị giác:
[`engine-render-contract.md`](../engine-render-contract.md).

**Slot dùng:** `number-bond-tree` hoặc `ten-frame-split`; slot `target` cho ô phần.

| Lớp | Engine này vẽ gì |
|---|---|
| 1. Nền cảnh | Cây tách gộp hoặc khung mười ô, vẽ trước |
| 2. Phần tử tĩnh | Ô tổng `whole` cố định, không chạm được |
| 3. Phần tử tương tác | Ô phần trống cộng dải `options` chạm được |
| 4. Lớp phản hồi | Ô phần sáng khi đúng; cả cây nhấp nháy nhẹ khi tổng khớp |

Trạng thái riêng: **tổng chưa khớp** — cây giữ nguyên, cấm báo sai cho tới khi trẻ điền đủ số phần.

**Thứ tự tuột khi thiết bị yếu:** theo mục 7.4 của hợp đồng chung. Lớp 3 và vùng chạm của nó
cấm — NEVER bỏ.

## 13. Ma trận seed mục tiêu

| Band | `count` | `compare` | `infer` | `plan` | Tổng mục tiêu |
|---|:--:|:--:|:--:|:--:|:--:|
| `3-4` | ≥3 | ≥1 | — | — | ≥4 |
| `4-5` | ≥3 | ≥2 | ≥1 | — | ≥6 |
| `5-6` | ≥2 | ≥2 | ≥2 | ≥1 | ≥7 |

Trục `what` mục tiêu: `number` · `quantity` · `arithmetic`.
Trục `theme` mục tiêu: ≥3 giá trị.
Hiện có 6 level, 2 giá trị `thinking`, **1 chủ đề**, 1 competency — đạt bậc 1 về số lượng,
thủng `theme_span` và `thinking_span` của bậc 2.

## 14. Ca sai không bắt được bằng schema

`whole` = 10 với `part_count` = 2 ở band 3-4. Parse sạch, `limits` sạch. Sai vì phạm vi số ở
band 3-4 là 1–5; trẻ chưa có biểu diễn lượng cho 10 nên bài trở thành đoán.

Ca thứ hai: `options` chứa hai cặp cùng đúng nhưng engine chỉ chấm một cặp — mâu thuẫn với
chính bản chất tách gộp.

## 15. Trường trích từ registry

Trích từ [`GT-007/template.ts`](../../../../packages/game-engine/src/templates/GT-007/template.ts).

| Trường | Giá trị |
|---|---|
| `mechanic` | `number-bond` |
| `layouts` | `number-bond-tree` · `ten-frame-split` |
| `age_min` · `age_max` | 3 · 6 |
| `banned_age_bands` | — |
| `requires_tap_fallback` | `true` |
| `asset_kinds` | `emoji` · `image` · `audio` |
| `engine_session` | `NumberBondSession` |

## 16. Chiều sâu nội dung

- `level_count`: cần đo, mục tiêu ≥6
- `min_band_count`: cần đo, mục tiêu ≥1
- `thinking_span`: cần đo, mục tiêu ≥2
- `what_span`: cần đo, mục tiêu ≥2
- `theme_span`: cần đo, mục tiêu ≥2
- `access_tier`: cần đo, ≥1 level `free` hoặc `login`

## 17. Miền hành vi

Từ vựng và luật: [`engine-behavior-domain.md`](../engine-behavior-domain.md).

**Miền chủ đạo:** `van-chuyen` · **Miền phụ:** — · **Cử chỉ chủ đạo:** `drop` ·
**Ràng buộc nhịp:** `tự do`

| Câu quan sát được | Band | Là bằng chứng của |
|---|:--:|---|
| "Con tách 5 hạt thành 2 và 3, rồi tự thử một cách khác" | `4-5` | Trẻ hiểu bài có **nhiều lời giải**, không đi tìm một đáp án |
| "Con đếm lại cả hai phần rồi gộp về xem có còn 5 không" | `3-4` | Bảo toàn lượng khi tách và gộp |
| "Con nói năm là hai với ba" | `5-6` | Diễn đạt quan hệ phần–tổng bằng lời |

**Điều kiện phát triển tiên quyết** (`BR-EBD-06`)

| Điều kiện | Ngưỡng band nhỏ nhất | Hệ quả thiết kế |
|---|:--:|---|
| Đếm | `3-4` cho tổng ≤5 | Tổng 10 chỉ mở ở `5-6` |
| Bộ nhớ làm việc | `3-4` | Tổng luôn hiện trên màn, trẻ không phải nhớ |
| Vận động | `3-4` **chỉ khi** có lối chạm hai lần | `requires_tap_fallback = true` |
| Ngôn ngữ | `4-5` | Từ "tách", "gộp" nói kèm động tác, không đứng một mình |

**Bậc biểu diễn theo band** (`BR-EBD-12`): `3-4` hạt và thanh số — vật đếm được · `4-5` vật
kèm chữ số · `5-6` chữ số trên hai phần.

**Kênh thắng cuộc** (`BR-EBD-07`): `hình` · `ký hiệu` — hạt và thanh số là lượng nhìn thấy được, chữ số trên hai phần nói giá trị tách gộp

## 18. Trục biến thể và độ mở

| Trục | Giá trị dùng ở engine này | Có trong ma trận mục 13 |
|---|---|:--:|
| `boi-canh` | **1 chủ đề hiện có** — mục 13 đòi ≥3, đây là chỗ thủng đã ghi ở mục 16 | Có |
| `kenh-de` | hạt màu · thanh số · chữ số | Chưa |
| `vai-tro` | người chia phần cho hai bạn · người kiểm lại cách chia của bạn | Chưa |
| `so-duong-thang` | **mọi cách tách đúng đều thắng** | Chưa |

**Độ mở (`do_mo`):** `bán mở` — và là engine `van-chuyen` mở nhất: một tổng có nhiều cách
tách, engine chấp nhận hết. Biến thể "tách 5 thành hai phần, con chọn cách nào cũng được" là
lối chơi mở sẵn có, không cần đổi contract.
**Quyền của trẻ (`BR-EBD-09`):** chọn cách tách; thử nhiều cách trong một lượt mà không bị
tính là sai.
