---
sheet: GT-018
engine: listen-respond
batch: legacy-v1
status: draft
reviewed: 2026-08-29
---

# GT-018 — Nghe rồi làm

## 1. Engine này dạy trẻ làm gì

Trẻ nghe một chỉ dẫn bằng âm thanh rồi thực hiện: chạm một vật, chạm nhiều vật theo thứ tự,
hoặc chọn theo mô tả. Đây là engine duy nhất trong corpus lấy **kênh nghe làm kênh chính** —
mọi engine khác đưa nội dung qua mắt.

Vì vậy nó là engine trung tâm của C5 ngôn ngữ, competency đang ít nội dung nhất (23 level).

## 2. Cơ chế và layout

Trích từ [`GT-018/template.ts:34`](../../../../packages/game-engine/src/templates/GT-018/template.ts).

| | |
|---|---|
| `mechanic` | `listen-respond` |
| `layouts` | `grid` · `horizontal-row` · `flex-wrap` |
| `engine_session` | `GT018Session` |
| `asset_kinds` | `emoji` · `image` · `audio` |
| `limits` | `item_count` [2, 8] · `distractor_count` [1, 4] · `target_count` [1, 4] |

## 3. Band tuổi và khả năng tiếp cận

| | |
|---|---|
| `age_min` · `age_max` | 4 · 6 |
| Band hợp lệ | `4-5` · `5-6` |
| `requires_tap_fallback` | Không |

**Cảnh báo:** corpus có 1 level `GT-018` gắn band `3-4` — vi phạm `BR-ECD-13`.

Engine phụ thuộc âm thanh nên nó là ca kiểm quan trọng của
[`accessibility.md`](../../08-quality/accessibility.md): thiết bị tắt tiếng phải có đường đi
thay thế, không phải màn hình đứng im.

## 4. Hình dạng `content_pack`

Khoá bậc một: `prompt` · `prompt_audio_ref` · `audio_prompt` · `response_mode` ·
`target_sequence` · `options`.

## 5. Trục độ khó

`hint_after_ms` · `allow_retry` · `auto_play_audio`.

## 6. Ma trận seed mục tiêu

| Band | `recall` | `sequence` | `inhibit` | `observe` | Tổng mục tiêu |
|---|:--:|:--:|:--:|:--:|:--:|
| `4-5` | ≥2 | ≥2 | ≥1 | ≥1 | ≥6 |
| `5-6` | ≥2 | ≥2 | ≥1 | ≥1 | ≥6 |

Trục `what` mục tiêu: `vocabulary` · `sound` · `category` · `space`.
Hiện có 3 level, 2 giá trị `thinking`, 2 chủ đề — mức mẫu, thủng bậc 1.

## 7. Ca sai không bắt được bằng schema

`target_sequence` dài 4 ở band 4-5. Parse sạch, trong `limits`. Sai vì nhớ chuỗi bốn chỉ dẫn
nghe được là bài kiểm trí nhớ làm việc, không phải bài ngôn ngữ; lứa 4–5 giữ được hai đến ba.

Ca thứ hai: `audio_prompt` dài quá 6 giây. Trẻ mất phần đầu trước khi câu kết thúc.

## 8. Acceptance criteria riêng

```gherkin
Scenario: GT-018 — nghe lại được không giới hạn
  Given một phiên GT-018 đang chạy
  When trẻ chạm nút nghe lại ba lần
  Then audio_prompt phát lại đủ ba lần
  And không lần nào tính là một lần sai

Scenario: GT-018 — thiết bị tắt tiếng vẫn chơi được
  Given âm lượng thiết bị bằng 0
  When phiên dựng
  Then nội dung chỉ dẫn hiện ra dạng chữ và hình
```

## 9. Boundaries

**Always** — cho nghe lại không giới hạn; có đường đi khi thiết bị tắt tiếng; `audio_prompt` ≤6 giây.

**Ask first** — `target_sequence` dài 4 ở band 5-6; `auto_play_audio` tắt.

**Never** — dùng engine này dưới band 4-5; phạt trẻ vì nghe lại.

## 10. Câu hỏi còn mở

| # | Câu hỏi | Chặn gì | Chặn phase | Chủ |
|---|---|---|---|---|
| 1 | C5 ngôn ngữ đang ít nội dung nhất và engine chính của nó ở mức mẫu. Lô nội dung đầu tiên có nên ưu tiên engine này không? | Thứ tự lô nội dung | P4 | Nội dung |
| 2 | 745 tệp âm thanh giọng Việt của v1 có tái dùng được cho `audio_prompt` không? Chúng nằm ngoài repo hiện tại | Chi phí soạn nội dung engine này | P4 | Nội dung |

## 11. Hợp đồng vẽ

Hợp đồng chung, gồm bốn lớp và năm trạng thái thị giác:
[`engine-render-contract.md`](../engine-render-contract.md).

**Slot dùng:** `grid`, `horizontal-row` hoặc `flex-wrap`.

| Lớp | Engine này vẽ gì |
|---|---|
| 1. Nền cảnh | Nền phẳng cộng chỉ báo âm thanh đang phát |
| 2. Phần tử tĩnh | Nút nghe lại, luôn hiện và luôn nhấn được |
| 3. Phần tử tương tác | 2–8 lựa chọn; ở `response_mode` theo chuỗi, thứ tự chạm hiện thành số nhỏ trên thẻ |
| 4. Lớp phản hồi | Hạt khi đủ chuỗi đúng |

Trạng thái riêng: **đang phát âm thanh** phải có chỉ báo nhìn thấy được, vì thiết bị tắt tiếng vẫn phải chơi được. Số thứ tự trên thẻ là kênh thị giác của chuỗi.

**Thứ tự tuột khi thiết bị yếu:** theo mục 7.4 của hợp đồng chung. Lớp 3 và vùng chạm của nó
cấm — NEVER bỏ.
