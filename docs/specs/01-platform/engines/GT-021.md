---
spec: ENGINE-GT021
title: Hoàn thiện đối xứng — mirror-complete
area: platform
status: approved
mvp: true
phase: P4
reviewed: 2026-08-30
engine: mirror-complete
batch: legacy-v1
owns:
  - Hợp đồng nội dung của engine GT-021
  - Hợp đồng vẽ của engine GT-021
  - Ma trận seed mục tiêu của engine GT-021
depends_on:
  - GAME-TEMPLATE-CONTRACT
  - GAME-LAYOUT-ENGINE
  - ENGINE-RENDER-CONTRACT
  - ENGINE-SPEC-SHEET
---

# GT-021 — Hoàn thiện đối xứng

## 1. Objective

Trẻ nhìn nửa hình bên này trục và điền nửa còn lại. Engine này dạy **đối xứng gương** như một
quy tắc sinh ra được, không phải như một từ. Trẻ phải dự đoán vị trí trước khi đặt, nên nó
là một trong bốn nguồn của trục `predict`.

## 2. Actors

| Actor | Quyền cần | Làm được gì ở đây |
|---|---|---|
| Trẻ 4–6 | — | Tương tác với engine mirror-complete |
| Người soạn nội dung | `content_reviewer` | Soạn `content_pack` và `difficulty_params` |
| Cổng | — | Kiểm tra tính hợp lệ |

## 3. Entry points

| Route / màn hình | Actor | Ghi chú |
|---|---|---|
| `packages/game-engine/src/templates/GT-021/template.ts` | Dev | Định nghĩa contract |
| `packages/game-engine/src/templates/GT-021/session.ts` | Dev | Logic + render |
| `docs/specs/01-platform/engines/GT-021.md` | Dev | Spec này |

## 4. Main flow

Bảy nhịp của [`engine-turn-script.md`](../engine-turn-script.md).

1. **`N1` Mở màn** — vẽ trục `axis` trước (dọc, ngang hoặc chéo), rồi `reference_pattern` ở
   nửa cho sẵn, rồi `target_slots` rỗng ở nửa kia, cuối cùng là `options`. Trục vẽ đầu tiên vì
   nó là luật của cả bài.
2. **`N2` Ra đề** — `prompt` đọc tự động một lần. Kênh hình song song: đường trục vẽ nét đứt
   và, khi `show_axis_guide` bật, có ánh phản chiếu mờ gợi ý phép soi gương.
   Lời đọc phát từ `instruction_audio_path` — một nguồn duy nhất (`BR-ETS-04`).
3. **`N3` Thao tác** — `drop` một phần tử từ `options` vào một ô của `target_slots`; `revert`
   bằng cách nhấc ra. Lối thay thế `tap` phần tử rồi `tap` ô.
4. **`N4` Phản hồi tức thì** — khi trẻ nhấc một phần tử, ô đối xứng với nó **sáng nhẹ** — gợi
   quan hệ mà không nói đáp án. Đặt xong thì phần tử bám ô; đặt lệch thì trôi về kèm nhịp hổ
   phách trên ô đối xứng, không trên phần tử.
5. **`N5` Trợ giúp** — quá `hint_after_ms`: `L1` một ô trống và ô nguồn đối xứng của nó cùng
   sáng · `L2` bóng mờ của phần tử đúng hiện trong ô · `L3` bàn tay ma đặt nó, chậm 0,5×.
6. **`N6` Kết lượt** — nửa còn lại khớp phép soi gương → `checkWinCondition()` trả `true`.
   Hình đối xứng hoàn chỉnh ở lại một nhịp, trục nháy một lần để trẻ thấy hai nửa cân nhau.
7. **`N7` Chơi lại** — thứ tự `options` đổi theo seed mới; `axis` và `reference_pattern` giữ
   nguyên; ô đích rỗng lại; trợ giúp về `L0`.

## 5. Alternative flows

Tám nhánh bắt buộc (`BR-ESS-19`), cộng nhánh riêng của engine.

| # | Nhánh | Điều kiện | Hành vi |
|---|---|---|---|
| 1 | Thao tác sai, còn lượt | Đặt sai ô, `allow_retry` bật | Phần tử trôi về, nhịp hổ phách trên **ô đối xứng đúng**; ô đã đúng giữ nguyên |
| 2 | Thao tác sai, hết lượt | `allow_retry` tắt | Lượt đóng; nửa còn thiếu hiện mờ để trẻ thấy hình trọn vẹn |
| 3 | Trẻ dừng lại | Quá `hint_after_ms` | Leo thang `L1`→`L3` như `N5` |
| 4 | Không nghe được lời đọc | Tắt âm hoặc thiếu giọng | Trục nét đứt và nửa cho sẵn là kênh đề; engine chơi được không lời |
| 5 | Asset hỏng | Ref không phân giải | Ô giữ chỗ; hai nửa cấm — NEVER dùng ô giữ chỗ khác nhau, đối xứng phải nhìn ra được |
| 6 | Thiết bị yếu · `prefers-reduced-motion` | FPS tụt | Bỏ hạt lớp 4; ánh sáng ô đối xứng ở `N4` cấm — NEVER bỏ, nó là giàn giáo của bài |
| 7 | Bỏ dở giữa lượt | Rời màn khi điền dở | Phiên `abandoned`; quay lại là lượt mới |
| 8 | Chơi lại lần thứ n | Trẻ bấm chơi lại | Thứ tự phương án đổi, trục và nửa cho sẵn giữ nguyên, trợ giúp về `L0` |

## 6. Business rules

| ID | Rule | Vì sao |
|---|---|---|
| `BR-E021-01` | Band hợp lệ: `4-5`, `5-6` | Phù hợp phát triển vận động và nhận thức của lứa tuổi |
| `BR-E021-02` | `checkWinCondition()` thuần — không side effect | Tính toán không được ảnh hưởng trạng thái |

## 7. Data

Contract đầy đủ nằm ở schema trong `template.ts` — cấm chép lại schema (`BR-ESS-03`).

## 8. API contract

Không có. Engine chạy trong tiến trình frontend/canvas runtime.

## 9. Acceptance criteria

```gherkin
Scenario: GT-021 — chỉ vị trí gương đúng được nhận
  Given một reference_pattern không đối xứng sẵn
  When trẻ đặt một phần tử vào vị trí không phải ảnh gương
  Then vị trí đó không được nhận

Scenario: GT-021 — tắt show_axis_guide không đổi đáp án
  Given cùng một content_pack
  When dựng phiên với show_axis_guide bật rồi tắt
  Then tập vị trí đúng không đổi

Scenario: BR-E021-01 — band hợp lệ
  Given một level GT-021
  When band nằm ngoài phạm vi cho phép
  Then cổng validate đỏ

Scenario: BR-E021-02 — checkWinCondition thuần
  Given một phiên GT-021
  When gọi checkWinCondition 100 lần
  Then trạng thái không đổi
```

## 10. Boundaries

**Always** — `reference_pattern` không tự đối xứng qua trục; trục dọc ở level đầu của một skill.

**Ask first** — trục ngang ở band 4-5; tắt `show_axis_guide` ở band 4-5.

**Never** — trục chéo ở lứa mầm non; mẫu tham chiếu đối xứng sẵn.

## 11. Open questions

| # | Câu hỏi | Chặn gì | Chặn phase | Chủ |
|---|---|---|---|---|
| 1 | Kiểm "mẫu tham chiếu không tự đối xứng" là phép kiểm hình học chạy được. Nó vào cổng seed hay vào bộ sinh? Cùng câu hỏi với `GT-019` | Cổng chất lượng nội dung | P5 | Backend |

## 12. Hợp đồng vẽ

Hợp đồng chung, gồm bốn lớp và năm trạng thái thị giác:
[`engine-render-contract.md`](../engine-render-contract.md).

**Slot dùng:** `mirror-axis-split`; nửa tham chiếu và nửa đích là hai vùng slot.

| Lớp | Engine này vẽ gì |
|---|---|
| 1. Nền cảnh | Trục đối xứng vẽ khi `show_axis_guide` bật; lưới ô hai bên |
| 2. Phần tử tĩnh | Nửa tham chiếu, không sửa được |
| 3. Phần tử tương tác | Ô đích trống cộng dải `options` |
| 4. Lớp phản hồi | Ô đúng sáng; khi đủ, cả hình nhấp nháy nhẹ một lần |

Trạng thái riêng: **ô gương của ô đang chạm** — chạm một ô ở nửa tham chiếu làm ô gương tương ứng sáng. Gợi ý cấu trúc, tắt được cùng `show_axis_guide`.

**Thứ tự tuột khi thiết bị yếu:** theo mục 7.4 của hợp đồng chung. Lớp 3 và vùng chạm của nó
cấm — NEVER bỏ.

## 13. Ma trận seed mục tiêu

| Band | `observe` | `predict` | `match` | `compare` | Tổng mục tiêu |
|---|:--:|:--:|:--:|:--:|:--:|
| `4-5` | ≥2 | ≥2 | ≥2 | — | ≥6 |
| `5-6` | ≥1 | ≥2 | ≥2 | ≥1 | ≥6 |

Trục `what` mục tiêu: `geometry` · `space` · `colour` · `pattern`.
Hiện có 3 level, 1 giá trị `thinking`, 2 chủ đề — mức mẫu, thủng bậc 1.

## 14. Ca sai không bắt được bằng schema

`reference_pattern` tự nó đã đối xứng qua trục. Parse sạch; sai vì bản sao gương trùng với
bản gốc, nên trẻ chép thẳng và vẫn đúng mà không dùng tới khái niệm đối xứng.

Ca thứ hai: trục chéo ở band 4-5. Trục dọc là trục tự nhiên nhất; trục ngang khó hơn; trục
chéo thuộc bậc sau lứa mầm non.

## 15. Trường trích từ registry

Trích từ [`GT-021/template.ts`](../../../../packages/game-engine/src/templates/GT-021/template.ts).

| Trường | Giá trị |
|---|---|
| `mechanic` | `mirror-complete` |
| `layouts` | `mirror-axis-split` |
| `age_min` · `age_max` | 4 · 6 |
| `banned_age_bands` | — |
| `requires_tap_fallback` | `true` |
| `asset_kinds` | `emoji` · `image` |
| `engine_session` | `GT021Session` |

## 16. Chiều sâu nội dung

<!-- @generated bởi scripts/gen-engine-depth-section.ts — Cấm sửa tay. -->

Sáu số đo hiện tại và mục tiêu bậc 1 (`BR-ECD-01`…`-06` — luật chiều sâu nội dung engine):

- `level_count`: hiện có 31, mục tiêu ≥6 (ĐẠT)
- `min_band_count`: hiện có 9 (band hợp lệ: `4-5`, `5-6`), mục tiêu ≥1 (ĐẠT)
- `thinking_span`: hiện có 7, mục tiêu ≥2 (ĐẠT)
- `what_span`: hiện có 6, mục tiêu ≥2 (ĐẠT)
- `theme_span`: hiện có 16, mục tiêu ≥2 (ĐẠT)
- `access_tier`: hiện có 18 level `free` hoặc `login`, mục tiêu ≥1 (ĐẠT)

## 17. Miền hành vi

Từ vựng và luật: [`engine-behavior-domain.md`](../engine-behavior-domain.md).

**Miền chủ đạo:** `sap-dat` · **Miền phụ:** `van-chuyen` ·
**Cử chỉ chủ đạo:** `drop` lặp · **Ràng buộc nhịp:** `tự do`

| Câu quan sát được | Band | Là bằng chứng của |
|---|:--:|---|
| "Con nhìn nửa bên trái rồi đặt đúng chỗ đối diện bên phải" | `4-5` | Ánh xạ qua trục — quy tắc sinh ra được, không phải nhớ |
| "Con đặt hụt một ô rồi tự kéo sang cho cân" | `4-5` | So sánh hai nửa và tự sửa |
| "Con đoán trước ô kế tiếp sẽ nằm ở đâu" | `5-6` | Dự đoán vị trí — một trong bốn nguồn của giá trị `predict` |

**Điều kiện phát triển tiên quyết** (`BR-EBD-06`)

| Điều kiện | Ngưỡng band nhỏ nhất | Hệ quả thiết kế |
|---|:--:|---|
| Ánh xạ không gian qua trục | `4-5` | Trục dọc trước, trục ngang sau — trục chéo chỉ ở `5-6` |
| Bộ nhớ làm việc | `4-5` | Giữ quan hệ đối xứng trong lúc đặt từng ô |
| Vận động | `3-4` **chỉ khi** có lối chạm hai lần | `requires_tap_fallback = true` |
| Ngôn ngữ | `4-5` | Không bắt buộc; từ "giống hệt bên kia" giúp nhưng không cần |

**Bậc biểu diễn theo band** (`BR-EBD-12`): `4-5` hình có nghĩa (con bướm, cái nhà) ·
`5-6` lưới hình học trần.

**Kênh thắng cuộc** (`BR-EBD-07`): `hình` — **NỢ**: nửa tham chiếu và ô đích đều chỉ là hình; trục đối xứng không được đánh dấu bằng kênh thứ hai

## 18. Trục biến thể và độ mở

| Trục | Giá trị dùng ở engine này | Có trong ma trận mục 13 |
|---|---|:--:|
| `boi-canh` | 2 chủ đề hiện có, mục 13 đòi thêm | Có |
| `kenh-de` | trục dọc · trục ngang · trục chéo · đối xứng màu | Có (qua trục `what`) |
| `vai-tro` | người hoàn thiện · người vẽ nửa hình cho bạn hoàn thiện | Chưa |
| `so-duong-thang` | một cấu hình đúng, thứ tự đặt tự do | Chưa |

**Độ mở (`do_mo`):** `đóng` — lý do cơ chế: đối xứng gương xác định **duy nhất** nửa còn lại;
hai lời giải nghĩa là hình gốc chưa đủ ràng buộc. Chỗ tự do là thứ tự đặt và đường tới.
**Quyền của trẻ (`BR-EBD-09`):** thứ tự ô; nhấc lại ô đã đặt; nộp khi tự thấy cân.
