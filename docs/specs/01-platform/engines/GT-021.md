---
spec: ENGINE-GT021
title: Hoàn thiện đối xứng — mirror-complete
area: platform
status: approved
mvp: true
phase: P4
reviewed: 2026-08-30
engine: mirror-complete
batch: legacy-v1
owns:
  - Hợp đồng nội dung của engine GT-021
  - Hợp đồng vẽ của engine GT-021
  - Ma trận seed mục tiêu của engine GT-021
depends_on:
  - GAME-TEMPLATE-CONTRACT
  - GAME-LAYOUT-ENGINE
  - ENGINE-RENDER-CONTRACT
  - ENGINE-SPEC-SHEET
---

# GT-021 — Hoàn thiện đối xứng

## 1. Objective

Trẻ nhìn nửa hình bên này trục và điền nửa còn lại. Engine này dạy **đối xứng gương** như một
quy tắc sinh ra được, không phải như một từ. Trẻ phải dự đoán vị trí trước khi đặt, nên nó
là một trong bốn nguồn của trục `predict`.

## 2. Actors

| Actor | Quyền cần | Làm được gì ở đây |
|---|---|---|
| Trẻ 4–6 | — | Tương tác với engine mirror-complete |
| Người soạn nội dung | `content_reviewer` | Soạn `content_pack` và `difficulty_params` |
| Cổng | — | Kiểm tra tính hợp lệ |

## 3. Entry points

| Route / màn hình | Actor | Ghi chú |
|---|---|---|
| `packages/game-engine/src/templates/GT-021/template.ts` | Dev | Định nghĩa contract |
| `packages/game-engine/src/templates/GT-021/session.ts` | Dev | Logic + render |
| `docs/specs/01-platform/engines/GT-021.md` | Dev | Spec này |

## 4. Main flow

1. Vòng lặp nạp `content_pack` đã parse bằng schema.
2. `setupEntities()` khởi tạo trạng thái.
3. Trẻ tương tác theo cơ chế `mirror-complete`.
4. `validateAction` kiểm tra hành động.
5. Phản hồi thị giác theo trạng thái.
6. `checkWinCondition()` trả `true` khi hoàn thành — thuần, không side effect.

## 5. Alternative flows

| Nhánh | Điều kiện | Hành vi |
|---|---|---|
| Sai | Trẻ thao tác sai | Phản hồi amber nudge, phiên vẫn chạy |
| Hết giờ gợi ý | Timeout | Scaffolding highlight |
| Asset hỏng | Emoji/image fail | Vẽ ô giữ chỗ |
| Thiết bị yếu | FPS tụt | Bỏ hạt lớp 4, giữ lớp 3 |

## 6. Business rules

| ID | Rule | Vì sao |
|---|---|---|
| `BR-E021-01` | Band hợp lệ: `3-4`, `4-5`, `5-6` | Phù hợp phát triển vận động và nhận thức của lứa tuổi |
| `BR-E021-02` | `checkWinCondition()` thuần — không side effect | Tính toán không được ảnh hưởng trạng thái |

## 7. Data

Contract đầy đủ nằm ở schema trong `template.ts` — cấm chép lại schema (`BR-ESS-03`).

## 8. API contract

Không có. Engine chạy trong tiến trình frontend/canvas runtime.

## 9. Acceptance criteria

```gherkin
Scenario: GT-021 — chỉ vị trí gương đúng được nhận
  Given một reference_pattern không đối xứng sẵn
  When trẻ đặt một phần tử vào vị trí không phải ảnh gương
  Then vị trí đó không được nhận

Scenario: GT-021 — tắt show_axis_guide không đổi đáp án
  Given cùng một content_pack
  When dựng phiên với show_axis_guide bật rồi tắt
  Then tập vị trí đúng không đổi

Scenario: BR-E021-01 — band hợp lệ
  Given một level GT-021
  When band nằm ngoài phạm vi cho phép
  Then cổng validate đỏ

Scenario: BR-E021-02 — checkWinCondition thuần
  Given một phiên GT-021
  When gọi checkWinCondition 100 lần
  Then trạng thái không đổi
```

## 10. Boundaries

**Always** — `reference_pattern` không tự đối xứng qua trục; trục dọc ở level đầu của một skill.

**Ask first** — trục ngang ở band 4-5; tắt `show_axis_guide` ở band 4-5.

**Never** — trục chéo ở lứa mầm non; mẫu tham chiếu đối xứng sẵn.

## 11. Open questions

| # | Câu hỏi | Chặn gì | Chặn phase | Chủ |
|---|---|---|---|---|
| 1 | Kiểm "mẫu tham chiếu không tự đối xứng" là phép kiểm hình học chạy được. Nó vào cổng seed hay vào bộ sinh? Cùng câu hỏi với `GT-019` | Cổng chất lượng nội dung | P5 | Backend |

## 12. Hợp đồng vẽ

Hợp đồng chung, gồm bốn lớp và năm trạng thái thị giác:
[`engine-render-contract.md`](../engine-render-contract.md).

**Slot dùng:** `mirror-axis-split`; nửa tham chiếu và nửa đích là hai vùng slot.

| Lớp | Engine này vẽ gì |
|---|---|
| 1. Nền cảnh | Trục đối xứng vẽ khi `show_axis_guide` bật; lưới ô hai bên |
| 2. Phần tử tĩnh | Nửa tham chiếu, không sửa được |
| 3. Phần tử tương tác | Ô đích trống cộng dải `options` |
| 4. Lớp phản hồi | Ô đúng sáng; khi đủ, cả hình nhấp nháy nhẹ một lần |

Trạng thái riêng: **ô gương của ô đang chạm** — chạm một ô ở nửa tham chiếu làm ô gương tương ứng sáng. Gợi ý cấu trúc, tắt được cùng `show_axis_guide`.

**Thứ tự tuột khi thiết bị yếu:** theo mục 7.4 của hợp đồng chung. Lớp 3 và vùng chạm của nó
cấm — NEVER bỏ.

## 13. Ma trận seed mục tiêu

| Band | `observe` | `predict` | `match` | `compare` | Tổng mục tiêu |
|---|:--:|:--:|:--:|:--:|:--:|
| `4-5` | ≥2 | ≥2 | ≥2 | — | ≥6 |
| `5-6` | ≥1 | ≥2 | ≥2 | ≥1 | ≥6 |

Trục `what` mục tiêu: `geometry` · `space` · `colour` · `pattern`.
Hiện có 3 level, 1 giá trị `thinking`, 2 chủ đề — mức mẫu, thủng bậc 1.

## 14. Ca sai không bắt được bằng schema

`reference_pattern` tự nó đã đối xứng qua trục. Parse sạch; sai vì bản sao gương trùng với
bản gốc, nên trẻ chép thẳng và vẫn đúng mà không dùng tới khái niệm đối xứng.

Ca thứ hai: trục chéo ở band 4-5. Trục dọc là trục tự nhiên nhất; trục ngang khó hơn; trục
chéo thuộc bậc sau lứa mầm non.

## 15. Trường trích từ registry

Trích từ [`GT-021/template.ts`](../../../../packages/game-engine/src/templates/GT-021/template.ts).

| Trường | Giá trị |
|---|---|
| `mechanic` | `mirror-complete` |
| `layouts` | `mirror-axis-split` |
| `age_min` · `age_max` | 4 · 6 |
| `banned_age_bands` | — |
| `requires_tap_fallback` | `true` |
| `asset_kinds` | `emoji` · `image` |
| `engine_session` | `GT021Session` |

## 16. Chiều sâu nội dung

- `level_count`: cần đo, mục tiêu ≥6
- `min_band_count`: cần đo, mục tiêu ≥1
- `thinking_span`: cần đo, mục tiêu ≥2
- `what_span`: cần đo, mục tiêu ≥2
- `theme_span`: cần đo, mục tiêu ≥2
- `access_tier`: cần đo, ≥1 level `free` hoặc `login`
