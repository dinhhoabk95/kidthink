---
spec: ENGINE-GT035
title: Xếp hàng lệnh — command-sequence
area: platform
status: published
mvp: false
phase: P4
reviewed: 2026-08-31
engine: command-sequence
batch: legacy-v1
owns:
  - Hợp đồng nội dung của engine GT-035
  - Hợp đồng vẽ của engine GT-035
  - Ma trận seed mục tiêu của engine GT-035
depends_on:
  - GAME-TEMPLATE-CONTRACT
  - GAME-LAYOUT-ENGINE
  - ENGINE-RENDER-CONTRACT
  - ENGINE-SPEC-SHEET
---

# GT-035 — Xếp hàng lệnh

> Phiếu **đặt trước** (`BR-ESS-15`). Plan sở hữu:
> [`188-engine-gt-035-command-sequence-plan.md`](../../../tasks/188-engine-gt-035-command-sequence-plan.md).
> Nguồn v1 gánh: `legacy_v1_ref: "D6-05"` (Code Đường đi — Unplugged).

## 1. Objective

Trẻ xếp trước một hàng lệnh (`forward`, `turn_left`, `turn_right`, `loop`), **rồi mới** bấm chạy,
và chỉ lúc đó mới biết đúng sai.

Khác `GT-013` ở chỗ phản hồi bị **hoãn lại**. `GT-013` là tìm đường: trẻ kéo ngón tay theo lối đi
và thấy ngay mình đang ở đâu. Ở đây trẻ phải chạy chương trình trong đầu trước khi máy chạy nó —
đó chính là thứ dạy lập kế hoạch, và là lý do bài này cấm — NEVER gộp vào khuôn mê cung.

## 2. Actors

| Actor | Quyền cần | Làm được gì ở đây |
|---|---|---|
| Trẻ 5–6 | — | Soạn hàng lệnh, chạy, xem robot đi từng bước |
| Người soạn nội dung | `content_reviewer` | Soạn `content_pack` và `difficulty_params` |
| Bộ sinh level | — | Sinh lưới có lời giải trong trần lệnh, kiểm bằng chính trình chạy |
| Cổng | — | Kiểm seed, contract và hợp đồng vẽ |

## 3. Entry points

| Route / màn hình | Actor | Ghi chú |
|---|---|---|
| `packages/game-engine/src/templates/GT-035/template.ts` | Dev | Contract — task #188 dựng |
| `packages/game-engine/src/templates/GT-035/session.ts` | Dev | Dựng trên nguyên thuỷ `ordering` |
| `packages/game-engine/src/systems/command-queue-system.ts` | Dev | Hệ thống mới, hai chế độ tách bạch |
| `docs/specs/01-platform/engines/GT-035.md` | mọi actor | Phiếu này |

## 4. Main flow

Bảy nhịp của [`engine-turn-script.md`](../engine-turn-script.md).

1. **`N1` Mở màn** — vẽ `grid` với `obstacles` và `collectibles`, rồi nhân vật ở `start` đúng
   hướng `facing`, rồi `goal`, cuối cùng là khay `allowed_commands` và hàng lệnh rỗng. Bản đồ
   hiện trước lệnh: trẻ phải khảo sát đường trước khi lập trình.
2. **`N2` Ra đề** — `prompt` đọc tự động một lần. Kênh hình song song: `goal` có cờ,
   `collectibles` có ánh sáng nhẹ, và ô đếm `max_commands` cho biết được dùng mấy lệnh.
   Lời đọc phát từ `instruction_audio_path` — một nguồn duy nhất (`BR-ETS-04`).
3. **`N3` Thao tác** — `drop` thẻ lệnh vào hàng lệnh, `revert` gỡ thẻ ra, `commit` bằng nút
   chạy. Lối thay thế `tap` thẻ rồi `tap` vị trí trong hàng.
4. **`N4` Phản hồi tức thì** — thẻ lệnh vào hàng thì nhân vật **xoay đầu nhìn** theo hướng của
   lệnh cuối cùng vừa thêm — phản hồi tức thì duy nhất có thể có khi kết quả bị hoãn. Khi chạy,
   mỗi lệnh thực thi một bước, thẻ đang chạy sáng lên.
5. **`N5` Trợ giúp** — quá `hint_after_ms`: `L1` ô kế tiếp trên một đường hợp lệ sáng · `L2`
   thẻ lệnh tương ứng với bước đó nháy trong khay · `L3` bàn tay ma đặt ba thẻ đầu vào hàng,
   chậm 0,5×.
6. **`N6` Kết lượt** — chạy xong và nhân vật ở `goal` (đã nhặt đủ `collectibles` nếu bài yêu
   cầu) → `checkWinCondition()` trả `true`. Hàng lệnh của trẻ ở lại một nhịp, sáng lần lượt
   theo đường vừa đi — trẻ đọc lại chương trình của mình.
7. **`N7` Chơi lại** — bản đồ mới theo seed nếu level cấp nhiều bố cục; `max_commands` và
   `allow_loop` giữ nguyên; hàng lệnh xoá sạch; trợ giúp về `L0`.

## 5. Alternative flows

Tám nhánh bắt buộc (`BR-ESS-19`), cộng nhánh riêng của engine.

| # | Nhánh | Điều kiện | Hành vi |
|---|---|---|---|
| 1 | Thao tác sai, còn lượt | Chạy xong mà không tới đích, `allow_retry` bật | Nhân vật về `start`; **hàng lệnh giữ nguyên** để trẻ sửa đúng chỗ sai |
| 2 | Thao tác sai, hết lượt | `allow_retry` tắt | Lượt đóng; một hàng lệnh hợp lệ chạy sáng một lần |
| 3 | Trẻ dừng lại | Quá `hint_after_ms` | Leo thang `L1`→`L3` như `N5` |
| 4 | Không nghe được lời đọc | Tắt âm hoặc thiếu giọng | Cờ đích, ánh sáng vật nhặt và ô đếm lệnh là kênh đề |
| 5 | Asset hỏng | Ref không phân giải | Ô giữ chỗ; thẻ lệnh vẽ bằng mũi tên nguyên thuỷ nên cấm — NEVER phụ thuộc asset |
| 6 | Thiết bị yếu · `prefers-reduced-motion` | FPS tụt | Bỏ hạt lớp 4; bước đi của nhân vật cấm — NEVER nhảy tức thì, trẻ phải theo dõi được |
| 7 | Bỏ dở giữa lượt | Rời màn khi xếp lệnh dở | Phiên `abandoned`; quay lại là lượt mới, hàng lệnh trống |
| 8 | Chơi lại lần thứ n | Trẻ bấm chơi lại | Bản đồ mới nếu có; **hàng lệnh khác vẫn thắng** khi cùng tới đích |

## 6. Business rules

| ID | Rule | Vì sao |
|---|---|---|
| `BR-E035-01` | Chế độ soạn cấm — NEVER chấm, cấm — NEVER báo đúng sai từng lệnh | Chấm trong lúc soạn biến bài lập kế hoạch thành bài thử sai, và xoá đúng thứ bài này đo |
| `BR-E035-02` | `refine` phải chứng minh tồn tại chuỗi lệnh ≤ `max_commands` đi hết `collectibles` tới `goal` không đâm `obstacles`, và chạy bằng **chính** trình chạy của session | Hai bản trình chạy sẽ lệch nhau; khi đó bộ sinh nói có lời giải còn trẻ thì không đi được |
| `BR-E035-03` | `max_commands` ≤ 8 | Trần của v1, giữ nguyên vì là ràng buộc sư phạm: hàng lệnh dài hơn thì trẻ không giữ nổi trong đầu, và bài quay về thử sai |
| `BR-E035-04` | `loop` lặp N lần khối liền trước; cấm — NEVER lồng `loop` trong `loop` | Lồng vòng lặp là khái niệm của bậc học sau; ở đây nó chỉ thêm bề mặt lỗi |
| `BR-E035-05` | Band cấm: `3-4` · `4-5` | Phản hồi hoãn lại đòi trẻ mô phỏng kết quả trong đầu, mốc của lứa 5–6 |

## 7. Data

**`content_pack`:** `prompt` · `grid` (`rows` 3–6 · `cols` 3–6) · `start`
(`col` + `row` + `facing`) · `goal` (`col` + `row` + `asset`) · `obstacles[]` (0–8) ·
`collectibles[]` (0–4, mỗi ô `asset`) · `allowed_commands[]` (tập con của `forward` `turn_left`
`turn_right` `loop`).
**`difficulty_params`:** `max_commands` (≤8) · `obstacle_count` · `collectible_count` ·
`allow_loop` · `allow_retry` · `hint_after_ms`.
**Band hợp lệ:** `5-6`. **Limits đặt trước:** mục 15.

Contract đầy đủ nằm ở schema trong `template.ts` — cấm — NEVER chép lại (`BR-ESS-03`).

## 8. API contract

Không có. Engine chạy trong tiến trình frontend/canvas runtime.

## 9. Acceptance criteria

```gherkin
Scenario: BR-E035-01 — soạn lệnh sai không sinh phản hồi đúng sai
  Given một level GT-035 và hàng lệnh đang rỗng
  When trẻ thêm ba lệnh dẫn robot đâm vật cản
  Then không phản hồi đúng sai nào phát ra
  And robot vẫn đứng nguyên ở ô start

Scenario: BR-E035-02 — lưới không có lời giải trong trần lệnh làm contract đỏ
  Given một lưới 6 nhân 6 mà đường đi ngắn nhất cần 11 lệnh
  And max_commands là 8
  When parse content_pack qua contract của GT-035
  Then refine từ chối vì không chuỗi lệnh nào trong trần tới được đích

Scenario: BR-E035-03 — max_commands vượt 8 làm contract đỏ
  Given difficulty_params khai max_commands là 10
  When parse difficulty_params qua contract của GT-035
  Then contract từ chối vì trần lệnh là 8

Scenario: BR-E035-04 — loop lồng trong loop làm contract đỏ
  Given một hàng lệnh mẫu chứa loop bên trong khối của một loop khác
  When trình chạy nhận hàng lệnh đó
  Then trình chạy từ chối và nêu rõ cấm lồng vòng lặp

Scenario: BR-E035-05 — band 4-5 làm cổng seed đỏ
  Given một level GT-035 gắn band 4-5
  When chạy cổng kiểm seed
  Then cổng thoát với mã khác 0
```

## 10. Boundaries

**Always**
- Tách bạch chế độ soạn và chế độ chạy.
- Cho sửa hàng lệnh sau mỗi lần chạy hỏng.
- Chỉ rõ lệnh thứ mấy làm hỏng chương trình.

**Ask first**
- Lưới vượt 6×6.
- Thêm lệnh ngoài bốn lệnh đang khai.

**Never**
- Chấm trong lúc soạn (`BR-E035-01`).
- Dựng trình giải thứ hai tách khỏi trình chạy (`BR-E035-02`).
- Lồng `loop` trong `loop` (`BR-E035-04`).
- Dùng engine này dưới band `5-6`.

## 11. Open questions

| # | Câu hỏi | Chặn gì | Chặn phase | Chủ |
|---|---|---|---|---|
| 1 | Robot đi theo hướng tương đối (`turn_left` xoay thân) hay tuyệt đối (mũi tên bốn hướng)? Tương đối đúng với nguồn v1 và dạy đổi hệ quy chiếu, nhưng khó hơn hẳn | Hợp đồng nội dung của `start.facing` | P4 | Nội dung |
| 2 | `loop` hiện số lần lặp bằng chữ số hay bằng số ô lặp lại? | Hợp đồng vẽ lớp 3 | P5 | Frontend |

## 12. Hợp đồng vẽ

Hợp đồng chung: [`engine-render-contract.md`](../engine-render-contract.md).

**Slot dùng:** `matrix-slot-grid` · `step-ladder`

| Lớp | Engine này vẽ gì |
|---|---|
| 1. Nền cảnh | Lưới ô vuông với vật cản, đích và vật cần nhặt |
| 2. Phần tử tĩnh | Câu lệnh; khay `allowed_commands`; số lệnh còn dùng được trên `max_commands` |
| 3. Phần tử tương tác | Hàng lệnh xếp được và bớt được; nút chạy; robot |
| 4. Lớp phản hồi | Hạt mừng khi tới đích đủ vật; rung hổ phách tại lệnh làm hỏng |

**Trạng thái thị giác riêng:** **đang chạy lệnh thứ i** — ô lệnh đang thi hành sáng viền, đồng bộ
với bước đi của robot, để trẻ nối được lệnh với hậu quả của nó. Trạng thái này là lõi sư phạm của
bài, cấm — NEVER bỏ khi tuột hiệu năng.
**Thứ tự tuột khi thiết bị yếu:** Lớp 4 (hạt) -> chuyển động trượt của robot (nhảy ô thay thế).
Hàng lệnh, viền lệnh đang chạy và vị trí robot cấm — NEVER bỏ.

## 13. Ma trận seed mục tiêu

| Band | `plan` | `predict` | `verify` | Tổng mục tiêu |
|---|:--:|:--:|:--:|:--:|
| `5-6` | ≥4 | ≥3 | ≥3 | ≥10 |

Trục `what` mục tiêu: `space` · `rule` · `pattern`.
Trục `theme` mục tiêu: ≥3 giá trị khác nhau trong 10 level của đợt đầu.

## 14. Ca sai không bắt được bằng schema

Lưới 5×5, không vật cản, không vật cần nhặt, `start` và `goal` nằm cùng hàng cách nhau ba ô.
Parse sạch, có lời giải trong trần. Sai vì lời giải là ba lệnh `forward` liên tiếp — không có chỗ
nào phải xoay, không có gì để lập kế hoạch, nên bài không đo được thứ nó tồn tại để đo. Mọi level
phải đòi ≥1 lần đổi hướng.

Ca thứ hai: `allow_loop` bật nhưng lời giải ngắn nhất không có đoạn nào lặp. Parse sạch; sai vì
lệnh `loop` nằm trong khay mà dùng vào chỉ làm hàng lệnh dài ra — trẻ học được rằng lặp là bẫy,
đúng ngược điều bài muốn dạy.

## 15. Trường trích từ registry

Chưa trích được — `packages/game-engine/src/templates/GT-035/template.ts` chưa tồn tại.
Bảng dưới là **giá trị đặt trước — `CHƯA ĐO`**, nguồn: plan #188 mục 2. Các cặp `limits` do người viết phiếu suy từ plan, **chưa có phép đo nào**.

**Luật giải quyết khi khuôn ra đời (`#205`): khuôn thắng.** Dev dựng `template.ts` theo nhu cầu thật của cơ chế, rồi **sửa phiếu theo** trong cùng PR. Cấm — NEVER bẻ khuôn cho khớp con số ở đây. Sổ số tạm: [`provisional-values.md`](../../../tasks/provisional-values.md).

| Trường | Giá trị đặt trước |
|---|---|
| `mechanic` | `command-sequence` |
| `layouts` | `matrix-slot-grid` · `step-ladder` |
| `age_min` · `age_max` | 5 · 6 |
| `banned_age_bands` | `3-4` · `4-5` |
| `requires_tap_fallback` | `true` |
| `limits` | `item_count` [1, 8] · `distractor_count` [0, 0] · `target_count` [1, 1] |
| `asset_kinds` | `emoji` · `image` |
| `engine_session` | `GT035Session` |

## 16. Chiều sâu nội dung

<!-- @generated bởi scripts/gen-engine-depth-section.ts — Cấm sửa tay. -->

Sáu số đo hiện tại và mục tiêu bậc 2 (`BR-ECD-01`…`-06` — luật chiều sâu nội dung engine):

- `level_count`: hiện có 17, mục tiêu ≥12 (ĐẠT)
- `min_band_count`: hiện có 17 (band hợp lệ: `5-6`), mục tiêu ≥3 (ĐẠT)
- `thinking_span`: hiện có 6, mục tiêu ≥3 (ĐẠT)
- `what_span`: hiện có 3, mục tiêu ≥3 (ĐẠT)
- `theme_span`: hiện có 8, mục tiêu ≥3 (ĐẠT)
- `access_tier`: hiện có 7 level `free` hoặc `login`, mục tiêu ≥1 (ĐẠT)

## 17. Miền hành vi

Từ vựng và luật: [`engine-behavior-domain.md`](../engine-behavior-domain.md).

**Miền chủ đạo:** `sap-dat` · **Miền phụ:** — · **Cử chỉ chủ đạo:** `drop` lặp + `commit` ·
**Ràng buộc nhịp:** `tự do`

Điều làm engine này khác `GT-013` cùng nói về đường đi: ở đây **phản hồi bị hoãn**. Trẻ xếp
xong cả hàng lệnh rồi mới bấm chạy, nên hành vi quan sát được là hành vi của người **lập kế
hoạch**, không phải người dò đường.

| Câu quan sát được | Band | Là bằng chứng của |
|---|:--:|---|
| "Con lấy ngón tay đi thử trên màn trước khi xếp lệnh" | `5-6` | Chạy chương trình trong đầu — mô phỏng trước khi làm |
| "Con xếp xong rồi đọc lại cả hàng lệnh trước khi bấm chạy" | `5-6` | Tự kiểm kế hoạch, không chạy thử bừa |
| "Con sửa đúng lệnh sai sau khi xem nhân vật đi chệch" | `5-6` | Truy ngược từ kết quả về bước gây lỗi |

**Điều kiện phát triển tiên quyết** (`BR-EBD-06`)

| Điều kiện | Ngưỡng band nhỏ nhất | Hệ quả thiết kế |
|---|:--:|---|
| Chịu được phản hồi hoãn | `5-6` | Trẻ nhỏ cần phản hồi tức thì — dòng cấm cả hai band nhỏ |
| Bộ nhớ làm việc | `5-6` | Giữ trạng thái tưởng tượng của nhân vật qua nhiều lệnh |
| Vận động | `3-4` **chỉ khi** có lối chạm hai lần | `requires_tap_fallback = true` |
| Ngôn ngữ | `5-6` | Từ chỉ hướng: tiến, rẽ trái, rẽ phải, lặp lại |

**Bậc biểu diễn theo band** (`BR-EBD-12`): `5-6` lệnh là thẻ có mũi tên — ký hiệu hướng, luôn
kèm hình nhân vật để lệnh không thành chữ trần.

**Kênh thắng cuộc** (`BR-EBD-07`): `hình` · `ký hiệu` — robot và cảnh là hình, thẻ lệnh mang mũi tên — ký hiệu hướng luôn kèm hình nhân vật

## 18. Trục biến thể và độ mở

| Trục | Giá trị dùng ở engine này | Có trong ma trận mục 13 |
|---|---|:--:|
| `boi-canh` | ≥3 chủ đề trong 10 level đợt đầu | Có |
| `kenh-de` | đích cho bằng hình · số lệnh bị giới hạn · có lệnh lặp | Có (qua trục `what`) |
| `vai-tro` | người lập trình · người sửa chương trình bạn xếp sai | Chưa |
| `so-duong-thang` | **nhiều hàng lệnh cùng tới đích** khi không giới hạn số lệnh | Chưa |

**Độ mở (`do_mo`):** `bán mở` — tới đích là điều kiện thắng, và thường có nhiều hàng lệnh cùng
tới. Siết số lệnh tối đa là cách đóng bài lại có chủ ý, và đó cũng là cách nâng độ khó mà không
đổi bản đồ.
**Quyền của trẻ (`BR-EBD-09`):** xếp và xoá lệnh tuỳ ý trước khi chạy; chạy lại bao nhiêu lần
cũng được; chọn đường đi của riêng mình.
