---
spec: ENGINE-GT003
title: Kéo vào đích — drag-to-container
area: platform
status: approved
mvp: true
phase: P4
reviewed: 2026-08-30
engine: drag-to-container
batch: mvp
owns:
  - Hợp đồng nội dung của engine GT-003
  - Hợp đồng vẽ của engine GT-003
  - Ma trận seed mục tiêu của engine GT-003
depends_on:
  - GAME-TEMPLATE-CONTRACT
  - GAME-LAYOUT-ENGINE
  - ENGINE-RENDER-CONTRACT
  - ENGINE-SPEC-SHEET
---

# GT-003 — Kéo vào đích

## 1. Objective

Trẻ kéo vật vào **một** đích chứa. Cơ chế mang theo một ý nghĩa mà chạm không có: hành động
"bỏ vào" là mô hình vận động của phép gộp, phép đếm có tích luỹ, và của việc thuộc về một
tập hợp. Đó là lý do engine này gánh phần lớn nội dung đếm của C1.

## 2. Actors

| Actor | Quyền cần | Làm được gì ở đây |
|---|---|---|
| Trẻ 3–6 | — | Tương tác với engine drag-to-container |
| Người soạn nội dung | `content_reviewer` | Soạn `content_pack` và `difficulty_params` |
| Cổng | — | Kiểm tra tính hợp lệ |

## 3. Entry points

| Route / màn hình | Actor | Ghi chú |
|---|---|---|
| `packages/game-engine/src/templates/GT-003/template.ts` | Dev | Định nghĩa contract |
| `packages/game-engine/src/templates/GT-003/session.ts` | Dev | Logic + render |
| `docs/specs/01-platform/engines/GT-003.md` | Dev | Spec này |

## 4. Main flow

1. Vòng lặp nạp `content_pack` đã parse bằng schema.
2. `setupEntities()` khởi tạo trạng thái.
3. Trẻ tương tác theo cơ chế `drag-to-container`.
4. `validateAction` kiểm tra hành động.
5. Phản hồi thị giác theo trạng thái.
6. `checkWinCondition()` trả `true` khi hoàn thành — thuần, không side effect.

## 5. Alternative flows

| Nhánh | Điều kiện | Hành vi |
|---|---|---|
| Sai | Trẻ thao tác sai | Phản hồi amber nudge, phiên vẫn chạy |
| Hết giờ gợi ý | Timeout | Scaffolding highlight |
| Asset hỏng | Emoji/image fail | Vẽ ô giữ chỗ |
| Thiết bị yếu | FPS tụt | Bỏ hạt lớp 4, giữ lớp 3 |

## 6. Business rules

| ID | Rule | Vì sao |
|---|---|---|
| `BR-E003-01` | Band hợp lệ: `3-4`, `4-5`, `5-6` | Phù hợp phát triển vận động và nhận thức của lứa tuổi |
| `BR-E003-02` | `checkWinCondition()` thuần — không side effect | Tính toán không được ảnh hưởng trạng thái |

## 7. Data

Contract đầy đủ nằm ở schema trong `template.ts` — cấm chép lại schema (`BR-ESS-03`).

## 8. API contract

Không có. Engine chạy trong tiến trình frontend/canvas runtime.

## 9. Acceptance criteria

```gherkin
Scenario: GT-003 — đường tap-tap tương đương đường kéo
  Given một level GT-003 ở band 3-4
  When trẻ chạm một item rồi chạm container
  Then item được đặt vào container
  And kết quả giống hệt khi kéo item vào container

Scenario: GT-003 — thả ngoài container trả item về chỗ cũ
  When trẻ thả một item ra vùng trống
  Then item quay về vị trí ban đầu
  And không tính là một lần sai

Scenario: BR-E003-01 — band hợp lệ
  Given một level GT-003
  When band nằm ngoài phạm vi cho phép
  Then cổng validate đỏ

Scenario: BR-E003-02 — checkWinCondition thuần
  Given một phiên GT-003
  When gọi checkWinCondition 100 lần
  Then trạng thái không đổi
```

## 10. Boundaries

**Always** — khai và test đường tap-tap; đích chứa lớn hơn vật rõ ràng.

**Ask first** — `distractor_count` > 2 ở band 3-4.

**Never** — bắt kéo chính xác vào vùng nhỏ hơn ngưỡng chạm tối thiểu; tính "thả trượt" là sai.

## 11. Open questions

| # | Câu hỏi | Chặn gì | Chặn phase | Chủ |
|---|---|---|---|---|
| 1 | Ngưỡng vùng thả ở band 3-4 lấy theo `getMinTouchTargetSize` hay lớn hơn? Trẻ 3 tuổi thả lệch nhiều hơn chạm lệch | Nội dung band 3-4 | P4 | Studio UI |

## 12. Hợp đồng vẽ

Hợp đồng chung, gồm bốn lớp và năm trạng thái thị giác:
[`engine-render-contract.md`](../engine-render-contract.md).

**Slot dùng:** `top-source-bottom-target` hoặc `left-source-right-target`; slot `source` cho vật, slot `target` cho đích chứa.

| Lớp | Engine này vẽ gì |
|---|---|
| 1. Nền cảnh | Vùng nguồn và vùng đích phân tách bằng khung theo token |
| 2. Phần tử tĩnh | Đích chứa vẽ bằng `drawClayContainer`, luôn lớn hơn vật rõ ràng |
| 3. Phần tử tương tác | 2–6 vật kéo được ở slot `source`; bản sao bán trong suốt bám ngón tay khi kéo |
| 4. Lớp phản hồi | Đích sáng lên khi vật đủ gần; hạt khi vật vào đúng |

Trạng thái riêng: **đích đang nhận** — viền đích dày lên khi vật ở trong bán kính thả. Đường tap-tap dùng cùng trạng thái đó sau cú chạm thứ nhất.

**Thứ tự tuột khi thiết bị yếu:** theo mục 7.4 của hợp đồng chung. Lớp 3 và vùng chạm của nó
cấm — NEVER bỏ.

## 13. Ma trận seed mục tiêu

| Band | `count` | `sort` | `match` | `observe` | Tổng mục tiêu |
|---|:--:|:--:|:--:|:--:|:--:|
| `3-4` | ≥3 | ≥1 | ≥2 | ≥2 | ≥8 |
| `4-5` | ≥3 | ≥2 | ≥2 | ≥2 | ≥9 |
| `5-6` | ≥2 | ≥2 | ≥2 | ≥2 | ≥8 |

Trục `what` mục tiêu: `number` · `quantity` · `colour` · `category`.
Hiện có 27 level, 6 giá trị `thinking`, 8 chủ đề — đạt bậc 2.

## 14. Ca sai không bắt được bằng schema

Một đích chứa vẽ bằng emoji nhỏ hơn vật được kéo. Parse sạch; sai vì trẻ không nhận ra cái
nhỏ chứa được cái to, và thất bại là thất bại về hình ảnh chứ không phải về nội dung.

Ca thứ hai: `distractor_count` = 4 với `item_count` = 2 ở band 3-4 — trẻ dành cả lượt chơi
để loại trừ thay vì để đếm.

## 15. Trường trích từ registry

Trích từ [`GT-003/template.ts`](../../../../packages/game-engine/src/templates/GT-003/template.ts).

| Trường | Giá trị |
|---|---|
| `mechanic` | `drag-to-container` |
| `layouts` | `top-source-bottom-target` · `left-source-right-target` |
| `age_min` · `age_max` | 3 · 6 |
| `banned_age_bands` | — |
| `requires_tap_fallback` | `true` |
| `asset_kinds` | `emoji` · `image` · `audio` |
| `engine_session` | `DragToContainerSession` |

## 16. Chiều sâu nội dung

- `level_count`: cần đo, mục tiêu ≥6
- `min_band_count`: cần đo, mục tiêu ≥1
- `thinking_span`: cần đo, mục tiêu ≥2
- `what_span`: cần đo, mục tiêu ≥2
- `theme_span`: cần đo, mục tiêu ≥2
- `access_tier`: cần đo, ≥1 level `free` hoặc `login`
