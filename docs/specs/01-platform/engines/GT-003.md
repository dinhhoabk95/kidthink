---
sheet: GT-003
engine: drag-to-container
batch: mvp
status: draft
reviewed: 2026-08-29
---

# GT-003 — Kéo vào đích

## 1. Engine này dạy trẻ làm gì

Trẻ kéo vật vào **một** đích chứa. Cơ chế mang theo một ý nghĩa mà chạm không có: hành động
"bỏ vào" là mô hình vận động của phép gộp, phép đếm có tích luỹ, và của việc thuộc về một
tập hợp. Đó là lý do engine này gánh phần lớn nội dung đếm của C1.

## 2. Cơ chế và layout

Trích từ [`GT-003/template.ts:35`](../../../../packages/game-engine/src/templates/GT-003/template.ts).

| | |
|---|---|
| `mechanic` | `drag-to-container` |
| `layouts` | `top-source-bottom-target` · `left-source-right-target` |
| `engine_session` | `DragToContainerSession` |
| `asset_kinds` | `emoji` · `image` · `audio` |
| `limits` | `item_count` [2, 6] · `distractor_count` [0, 4] · `target_count` [1, 4] |

## 3. Band tuổi và khả năng tiếp cận

| | |
|---|---|
| `age_min` · `age_max` | 3 · 6 |
| Band hợp lệ | `3-4` · `4-5` · `5-6` |
| `requires_tap_fallback` | **Có** — `BR-GTC-06` |

Đường tap-tap (chạm vật, chạm đích) là đường **chính** ở band 3-4, không phải đường dự phòng.
Kéo chính xác ở tuổi 3–4 là thiết kế sai, không phải độ khó cao.

## 4. Hình dạng `content_pack`

Khoá bậc một: `prompt` · `prompt_audio_ref` · `container` · `items`.

## 5. Trục độ khó

`distractor_count` · `target_count` · `hint_after_ms` · `allow_retry`.

## 6. Ma trận seed mục tiêu

| Band | `count` | `sort` | `match` | `observe` | Tổng mục tiêu |
|---|:--:|:--:|:--:|:--:|:--:|
| `3-4` | ≥3 | ≥1 | ≥2 | ≥2 | ≥8 |
| `4-5` | ≥3 | ≥2 | ≥2 | ≥2 | ≥9 |
| `5-6` | ≥2 | ≥2 | ≥2 | ≥2 | ≥8 |

Trục `what` mục tiêu: `number` · `quantity` · `colour` · `category`.
Hiện có 27 level, 6 giá trị `thinking`, 8 chủ đề — đạt bậc 2.

## 7. Ca sai không bắt được bằng schema

Một đích chứa vẽ bằng emoji nhỏ hơn vật được kéo. Parse sạch; sai vì trẻ không nhận ra cái
nhỏ chứa được cái to, và thất bại là thất bại về hình ảnh chứ không phải về nội dung.

Ca thứ hai: `distractor_count` = 4 với `item_count` = 2 ở band 3-4 — trẻ dành cả lượt chơi
để loại trừ thay vì để đếm.

## 8. Acceptance criteria riêng

```gherkin
Scenario: GT-003 — đường tap-tap tương đương đường kéo
  Given một level GT-003 ở band 3-4
  When trẻ chạm một item rồi chạm container
  Then item được đặt vào container
  And kết quả giống hệt khi kéo item vào container

Scenario: GT-003 — thả ngoài container trả item về chỗ cũ
  When trẻ thả một item ra vùng trống
  Then item quay về vị trí ban đầu
  And không tính là một lần sai
```

## 9. Boundaries

**Always** — khai và test đường tap-tap; đích chứa lớn hơn vật rõ ràng.

**Ask first** — `distractor_count` > 2 ở band 3-4.

**Never** — bắt kéo chính xác vào vùng nhỏ hơn ngưỡng chạm tối thiểu; tính "thả trượt" là sai.

## 10. Câu hỏi còn mở

| # | Câu hỏi | Chặn gì | Chặn phase | Chủ |
|---|---|---|---|---|
| 1 | Ngưỡng vùng thả ở band 3-4 lấy theo `getMinTouchTargetSize` hay lớn hơn? Trẻ 3 tuổi thả lệch nhiều hơn chạm lệch | Nội dung band 3-4 | P4 | Studio UI |

## 11. Hợp đồng vẽ

Hợp đồng chung, gồm bốn lớp và năm trạng thái thị giác:
[`engine-render-contract.md`](../engine-render-contract.md).

**Slot dùng:** `top-source-bottom-target` hoặc `left-source-right-target`; slot `source` cho vật, slot `target` cho đích chứa.

| Lớp | Engine này vẽ gì |
|---|---|
| 1. Nền cảnh | Vùng nguồn và vùng đích phân tách bằng khung theo token |
| 2. Phần tử tĩnh | Đích chứa vẽ bằng `drawClayContainer`, luôn lớn hơn vật rõ ràng |
| 3. Phần tử tương tác | 2–6 vật kéo được ở slot `source`; bản sao bán trong suốt bám ngón tay khi kéo |
| 4. Lớp phản hồi | Đích sáng lên khi vật đủ gần; hạt khi vật vào đúng |

Trạng thái riêng: **đích đang nhận** — viền đích dày lên khi vật ở trong bán kính thả. Đường tap-tap dùng cùng trạng thái đó sau cú chạm thứ nhất.

**Thứ tự tuột khi thiết bị yếu:** theo mục 7.4 của hợp đồng chung. Lớp 3 và vùng chạm của nó
cấm — NEVER bỏ.
