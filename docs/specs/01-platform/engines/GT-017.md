---
sheet: GT-017
engine: block-stack
batch: montessori
status: draft
reviewed: 2026-08-29
---

# GT-017 — Xếp khối và phối cảnh

## 1. Engine này dạy trẻ làm gì

Trẻ nhìn một khối lập phương xếp chồng và trả lời về nó: có bao nhiêu khối, nhìn từ trên
xuống thấy gì, khối nào bị che. Engine này dạy **suy ra cái không nhìn thấy từ cái nhìn
thấy** — bước đầu của tư duy không gian ba chiều.

## 2. Cơ chế và layout

Trích từ [`GT-017/template.ts:62`](../../../../packages/game-engine/src/templates/GT-017/template.ts).

| | |
|---|---|
| `mechanic` | `block-stack` |
| `layouts` | `split-columns` · `grid` |
| `engine_session` | `BlockStackSession` |
| `asset_kinds` | `emoji` · `image` · `audio` |
| `limits` | `item_count` [2, 6] · `distractor_count` [1, 5] · `target_count` [1, 1] |

## 3. Band tuổi và khả năng tiếp cận

| | |
|---|---|
| `age_min` · `age_max` | 5 · 6 |
| Band hợp lệ | `5-6` |
| `requires_tap_fallback` | Không |

## 4. Hình dạng `content_pack`

Khoá bậc một: `prompt` · `prompt_audio_ref` · `model` · `question` · `options`.

## 5. Trục độ khó

`hidden_cube_count` · `distractor_count` · `allow_rotate` · `hint_after_ms` · `allow_retry`.

`allow_rotate` đổi bản chất bài: bật thì trẻ kiểm chứng được, tắt thì trẻ phải suy ra. Level
đầu của một skill nên bật.

## 6. Ma trận seed mục tiêu

| Band | `observe` | `infer` | `predict` | `count` | Tổng mục tiêu |
|---|:--:|:--:|:--:|:--:|:--:|
| `5-6` | ≥4 | ≥3 | ≥3 | ≥2 | ≥12 |

Trục `what` mục tiêu: `space` · `geometry` · `quantity`.
Hiện có 3 level, 2 giá trị `thinking`, **1 chủ đề** — mức mẫu, thủng bậc 1.

## 7. Ca sai không bắt được bằng schema

`hidden_cube_count` = 3 với `allow_rotate` = false ở level đầu tiên của một skill. Parse sạch;
sai vì trẻ chưa có mô hình khối chồng và không có cách kiểm chứng, nên câu trả lời là đoán.

Ca thứ hai: mô hình vẽ ở góc nhìn mà hai khối chồng lên nhau thành một hình thang khó đọc.
Contract không nói gì về góc nhìn, nhưng góc nhìn quyết định bài có đọc được không.

## 8. Acceptance criteria riêng

```gherkin
Scenario: GT-017 — allow_rotate cho xoay đủ bốn mặt
  Given allow_rotate là true
  When trẻ xoay mô hình bốn lần
  Then mô hình trở về góc nhìn ban đầu

Scenario: GT-017 — hidden_cube_count khớp mô hình
  Given một content_pack với hidden_cube_count là 2
  When đếm khối bị che hoàn toàn trong model
  Then đếm được đúng 2
```

## 9. Boundaries

**Always** — bật `allow_rotate` ở level đầu của một skill; góc nhìn để mọi khối phân biệt được.

**Ask first** — `hidden_cube_count` > 2; tắt `allow_rotate` ở band 5-6.

**Never** — số khối bị che không khớp mô hình; góc nhìn làm hai khối chập thành một.

## 10. Câu hỏi còn mở

| # | Câu hỏi | Chặn gì | Chặn phase | Chủ |
|---|---|---|---|---|
| 1 | Góc nhìn có nên là trường trong `content_pack` không? Hiện nó nằm trong cách vẽ `model`, nên người soạn không kiểm soát được và cũng không kiểm được | Chất lượng nội dung engine này | P5 | Studio UI |

## 11. Hợp đồng vẽ

Hợp đồng chung, gồm bốn lớp và năm trạng thái thị giác:
[`engine-render-contract.md`](../engine-render-contract.md).

**Slot dùng:** `split-columns` hoặc `grid`; mô hình một bên, `options` một bên.

| Lớp | Engine này vẽ gì |
|---|---|
| 1. Nền cảnh | Nền phẳng cộng lưới đẳng cự cho mô hình khối |
| 2. Phần tử tĩnh | Mô hình khối vẽ bằng `isometric-system`; khối bị che vẽ nét đứt khi `allow_rotate` bật |
| 3. Phần tử tương tác | Nút xoay và 2–6 lựa chọn |
| 4. Lớp phản hồi | Lựa chọn đúng nở nhẹ kèm hạt |

Trạng thái riêng: **đang xoay** — mô hình quay mượt qua các góc 90 độ, cấm nhảy tức thời vì trẻ mất dấu khối nào là khối nào.

**Thứ tự tuột khi thiết bị yếu:** theo mục 7.4 của hợp đồng chung. Lớp 3 và vùng chạm của nó
cấm — NEVER bỏ.
