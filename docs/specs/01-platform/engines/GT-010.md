---
spec: ENGINE-GT010
title: Thay thế biểu tượng — substitution
area: platform
status: approved
mvp: true
phase: P4
reviewed: 2026-08-30
engine: substitution
batch: montessori
owns:
  - Hợp đồng nội dung của engine GT-010
  - Hợp đồng vẽ của engine GT-010
  - Ma trận seed mục tiêu của engine GT-010
depends_on:
  - GAME-TEMPLATE-CONTRACT
  - GAME-LAYOUT-ENGINE
  - ENGINE-RENDER-CONTRACT
  - ENGINE-SPEC-SHEET
---

# GT-010 — Thay thế biểu tượng

## 1. Objective

Trẻ học một luật thay thế ("hình vuông đứng cho 2 quả táo"), rồi áp luật đó vào một biểu thức
mới. Đây là bước đầu tiên của **tư duy ký hiệu**: một vật đứng thay cho vật khác, và quan hệ
đó giữ nguyên qua nhiều lần dùng.

Engine này là một trong ba nơi corpus sinh ra giá trị trục `shift` — chuyển giữa hai hệ quy
chiếu, vật thật và ký hiệu.

## 2. Actors

| Actor | Quyền cần | Làm được gì ở đây |
|---|---|---|
| Trẻ 4–6 | — | Tương tác với engine substitution |
| Người soạn nội dung | `content_reviewer` | Soạn `content_pack` và `difficulty_params` |
| Cổng | — | Kiểm tra tính hợp lệ |

## 3. Entry points

| Route / màn hình | Actor | Ghi chú |
|---|---|---|
| `packages/game-engine/src/templates/GT-010/template.ts` | Dev | Định nghĩa contract |
| `packages/game-engine/src/templates/GT-010/session.ts` | Dev | Logic + render |
| `docs/specs/01-platform/engines/GT-010.md` | Dev | Spec này |

## 4. Main flow

Bảy nhịp của [`engine-turn-script.md`](../engine-turn-script.md).

1. **`N1` Mở màn** — vẽ bảng luật (`symbols` và giá trị thay thế) ở một góc cố định, rồi
   `equations`, rồi `question`, cuối cùng là `options`. Bảng luật vẽ **trước** vì trẻ phải
   tra nó, không phải nhớ nó.
2. **`N2` Ra đề** — `prompt` đọc tự động một lần. Kênh hình song song: bảng luật ghim tại chỗ
   suốt lượt, mỗi ký hiệu đứng cạnh vật nó thay thế.
   Lời đọc phát từ `instruction_audio_path` — một nguồn duy nhất (`BR-ETS-04`).
3. **`N3` Thao tác** — `tap` vào một phương án trong `options`. Trẻ chạm vào bảng luật thì
   ký hiệu đó phóng to — thao tác tra cứu, không tính là trả lời.
4. **`N4` Phản hồi tức thì** — phương án chạm vào nảy nhẹ; đúng thì khoá và biểu thức tự
   thay ký hiệu bằng vật thật một lượt; sai thì nhịp hổ phách trên phương án.
5. **`N5` Trợ giúp** — quá `hint_after_ms`: `L1` dòng luật liên quan trong bảng sáng lên ·
   `L2` bàn tay ma chỉ từ ký hiệu trong câu hỏi sang dòng luật tương ứng · `L3` chậm 0,5×, lặp.
6. **`N6` Kết lượt** — chọn đúng → `checkWinCondition()` trả `true`. Biểu thức đã thay hết ký
   hiệu ở lại một nhịp: đó là lúc quan hệ thay thế nhìn thấy được.
7. **`N7` Chơi lại** — thứ tự `options` đổi theo seed mới; bảng luật, `equation_count` và
   `step_count` giữ nguyên; trợ giúp về `L0`.

## 5. Alternative flows

Tám nhánh bắt buộc (`BR-ESS-19`), cộng nhánh riêng của engine.

| # | Nhánh | Điều kiện | Hành vi |
|---|---|---|---|
| 1 | Thao tác sai, còn lượt | Chọn sai, `allow_retry` bật | Nhịp hổ phách trên phương án; bảng luật sáng nhẹ để mời tra lại |
| 2 | Thao tác sai, hết lượt | `allow_retry` tắt | Lượt đóng; biểu thức tự thay ký hiệu một lượt để trẻ thấy đáp án đến từ đâu |
| 3 | Trẻ dừng lại | Quá `hint_after_ms` | Leo thang `L1`→`L3` như `N5` |
| 4 | Không nghe được lời đọc | Tắt âm hoặc thiếu giọng | Bảng luật là kênh đề đầy đủ; engine chơi được không lời |
| 5 | Asset hỏng | Ref không phân giải | Ô giữ chỗ; ký hiệu và vật thay thế cấm — NEVER cùng rơi về một hình |
| 6 | Thiết bị yếu · `prefers-reduced-motion` | FPS tụt | Bỏ hạt lớp 4; lượt thay ký hiệu ở `N6` rút thành một khung tĩnh |
| 7 | Bỏ dở giữa lượt | Rời màn trước khi chọn | Phiên `abandoned`; quay lại là lượt mới |
| 8 | Chơi lại lần thứ n | Trẻ bấm chơi lại | Thứ tự phương án đổi, bảng luật giữ nguyên, trợ giúp về `L0` |

## 6. Business rules

| ID | Rule | Vì sao |
|---|---|---|
| `BR-E010-01` | Quy ước giá trị của biểu tượng (`symbols`) phải nhất quán tuyệt đối trong cả level; một ký hiệu không được mang hai giá trị khác nhau | Tính nhất quán của biến số là nguyên lý nền tảng của đại số sơ cấp; đổi nghĩa ký hiệu giữa chừng phá vỡ tư duy logic toán |
| `BR-E010-02` | Mọi ký hiệu xuất hiện trong phương trình (`question`) bắt buộc phải có mặt trong bảng quy ước `symbols` | Tránh trường hợp bài toán đưa ra ẩn số không có dữ kiện tra cứu, gây tắc nghẽn tiến trình giải toán |
| `BR-E010-03` | Giới hạn tối đa 3 loại biểu tượng đồng thời trong một bài toán | Dung lượng xử lý quy tắc thế của trẻ 4-6 tuổi có hạn; nhiều hơn 3 ký hiệu làm phân mảnh chú ý và biến bài học thành tra cứu cơ học |

## 7. Data

Contract đầy đủ nằm ở schema trong `template.ts` — cấm chép lại schema (`BR-ESS-03`).

## 8. API contract

Không có. Engine chạy trong tiến trình frontend/canvas runtime.

## 9. Acceptance criteria

```gherkin
Scenario: BR-E010-01 — luật thay thế nhất quán trong một level
  Given symbols khai hình vuông đại diện cho giá trị 2
  When kiểm tra mọi biểu thức trong content_pack
  Then không biểu thức nào dùng hình vuông với giá trị khác 2

Scenario: BR-E010-02 — question chỉ dùng ký hiệu đã khai báo
  When server parse content_pack
  Then mọi ký hiệu trong question đều phải có mặt trong symbols

Scenario: BR-E010-03 — giới hạn tối đa 3 loại biểu tượng
  Given một level GT-010
  When số lượng biểu tượng trong symbols vượt quá 3
  Then cổng kiểm tra sư phạm cảnh báo quá tải tra cứu đại số
```

## 10. Boundaries

**Always** — ký hiệu là hình trừu tượng; luật thay thế nhất quán trong cả level.

**Ask first** — `step_count` 2 ở band 4-5.

**Never** — dùng emoji mang nghĩa đời thật làm ký hiệu; đổi nghĩa một ký hiệu giữa chừng.

## 11. Open questions

| # | Câu hỏi | Chặn gì | Chặn phase | Chủ |
|---|---|---|---|---|
| 1 | Ranh giới với `GT-027` đổi luật giữa chừng: cả hai sinh `shift`. `GT-010` đổi hệ quy chiếu, `GT-027` đổi luật phân loại. Ranh giới đó có đủ rõ cho người soạn không? | Phân bổ nội dung trục `shift` | P5 | Nội dung |

## 12. Hợp đồng vẽ

Hợp đồng chung, gồm bốn lớp và năm trạng thái thị giác:
[`engine-render-contract.md`](../engine-render-contract.md).

**Slot dùng:** `equation-rows` hoặc `grid`; slot `neutral` cho ô đáp án.

| Lớp | Engine này vẽ gì |
|---|---|
| 1. Nền cảnh | Các hàng biểu thức xếp dọc, cách đều |
| 2. Phần tử tĩnh | Bảng ký hiệu ở đỉnh: mỗi ký hiệu kèm giá trị nó thay thế |
| 3. Phần tử tương tác | Dải `options` chạm được ở đáy |
| 4. Lớp phản hồi | Ký hiệu trong biểu thức sáng theo khi trẻ chạm ký hiệu ở bảng |

Trạng thái riêng: **ký hiệu đang tra** — chạm một ký hiệu ở bảng làm mọi lần xuất hiện của nó trong biểu thức sáng lên. Đây là gợi ý cấu trúc, không phải đáp án.

**Thứ tự tuột khi thiết bị yếu:** theo mục 7.4 của hợp đồng chung. Lớp 3 và vùng chạm của nó
cấm — NEVER bỏ.

## 13. Ma trận seed mục tiêu

| Band | `infer` | `match` | `recall` | `shift` | Tổng mục tiêu |
|---|:--:|:--:|:--:|:--:|:--:|
| `4-5` | ≥2 | ≥2 | ≥1 | ≥1 | ≥6 |
| `5-6` | ≥2 | ≥1 | ≥1 | ≥2 | ≥6 |

Trục `what` mục tiêu: `pattern` · `number` · `quantity`.
Hiện có 3 level, 1 giá trị `thinking`, 3 chủ đề — mức mẫu, thủng bậc 1.

## 14. Ca sai không bắt được bằng schema

Một `symbol` dùng emoji đã mang nghĩa sẵn với trẻ — ví dụ dùng 🍎 làm ký hiệu đứng thay cho
3 con mèo. Parse sạch; sai vì trẻ đọc quả táo là quả táo và không chuyển sang vai ký hiệu.
Ký hiệu của engine này nên là hình trừu tượng: hình học, màu, chấm.

## 15. Trường trích từ registry

Trích từ [`GT-010/template.ts`](../../../../packages/game-engine/src/templates/GT-010/template.ts).

| Trường | Giá trị |
|---|---|
| `mechanic` | `substitution` |
| `layouts` | `equation-rows` · `grid` |
| `age_min` · `age_max` | 4 · 6 |
| `banned_age_bands` | — |
| `requires_tap_fallback` | `false` |
| `asset_kinds` | `emoji` · `image` · `audio` |
| `engine_session` | `SubstitutionSession` |

## 16. Chiều sâu nội dung

<!-- @generated bởi scripts/gen-engine-depth-section.ts — Cấm sửa tay. -->

Sáu số đo hiện tại và mục tiêu bậc 1 (`BR-ECD-01`…`-06` — luật chiều sâu nội dung engine):

- `level_count`: hiện có 189, mục tiêu ≥6 (ĐẠT)
- `min_band_count`: hiện có 32 (band hợp lệ: `4-5`, `5-6`), mục tiêu ≥1 (ĐẠT)
- `thinking_span`: hiện có 12, mục tiêu ≥2 (ĐẠT)
- `what_span`: hiện có 11, mục tiêu ≥2 (ĐẠT)
- `theme_span`: hiện có 16, mục tiêu ≥2 (ĐẠT)
- `access_tier`: hiện có 39 level `free` hoặc `login`, mục tiêu ≥1 (ĐẠT)

## 17. Miền hành vi

Từ vựng và luật: [`engine-behavior-domain.md`](../engine-behavior-domain.md).

**Miền chủ đạo:** `chi-dinh` · **Miền phụ:** — · **Cử chỉ chủ đạo:** `tap` ·
**Ràng buộc nhịp:** `tự do`

| Câu quan sát được | Band | Là bằng chứng của |
|---|:--:|---|
| "Con nhìn bảng luật rồi mới nhìn đề" | `4-5` | Dùng luật như công cụ, không học thuộc |
| "Con nói hình vuông là hai quả táo" | `4-5` | Diễn đạt quan hệ thay thế bằng lời |
| "Con áp đúng luật ở biểu thức mới chưa từng thấy" | `5-6` | Chuyển hệ quy chiếu — nguồn của giá trị `shift` |

**Điều kiện phát triển tiên quyết** (`BR-EBD-06`)

| Điều kiện | Ngưỡng band nhỏ nhất | Hệ quả thiết kế |
|---|:--:|---|
| Tư duy ký hiệu | `4-5` | "Một vật đứng thay vật khác" là bước trừu tượng đầu tiên |
| Bộ nhớ làm việc | `4-5` | Bảng luật hiện thường trực trên màn, cấm bắt nhớ |
| Vận động | `3-4` | Một cú chạm |
| Ngôn ngữ | `4-5` | Câu lệnh có mệnh đề điều kiện |

**Bậc biểu diễn theo band** (`BR-EBD-12`): `4-5` vật thay bằng hình · `5-6` hình thay bằng ký
hiệu — engine này chính là cây cầu giữa hai bậc.

**Kênh thắng cuộc** (`BR-EBD-07`): `hình` · `ký hiệu` — vật là hình, bảng ký hiệu ở đỉnh nói mỗi ký hiệu thay cho giá trị nào

## 18. Trục biến thể và độ mở

| Trục | Giá trị dùng ở engine này | Có trong ma trận mục 13 |
|---|---|:--:|
| `boi-canh` | 3 chủ đề đã có trong corpus | Có |
| `kenh-de` | luật bằng hình · luật bằng chữ · luật bằng giọng | Chưa |
| `vai-tro` | người giải mã · người đặt mật mã cho bạn đoán | Chưa |
| `so-duong-thang` | một kết quả đúng sau khi áp luật | Có |

**Độ mở (`do_mo`):** `đóng` — lý do cơ chế: luật thay thế là hàm, và hàm chỉ cho một ảnh. Biến
thể **người đặt mật mã** ở trục `vai-tro` là lối mở duy nhất thấy được, và nó cần cơ chế của
`GT-036` chứ không phải của engine này.
**Quyền của trẻ (`BR-EBD-09`):** mở lại bảng luật bất cứ lúc nào; không có đồng hồ.
