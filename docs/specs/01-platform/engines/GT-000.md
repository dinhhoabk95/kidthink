---
spec: ENGINE-GT000
title: Làm quen khái niệm — concept-intro
area: platform
status: published
mvp: false
phase: P4
reviewed: 2026-09-05
engine: concept-intro
batch: core-pedagogy
owns:
  - Hợp đồng nội dung của engine GT-000
  - Hợp đồng vẽ của engine GT-000
  - Ma trận seed mục tiêu của engine GT-000
depends_on:
  - GAME-TEMPLATE-CONTRACT
  - GAME-LAYOUT-ENGINE
  - ENGINE-RENDER-CONTRACT
  - ENGINE-SPEC-SHEET
  - CONCEPT-TOPIC-MODEL
---

# GT-000 — Làm quen khái niệm

> Phiếu đặc tả template `GT-000` (`BR-ESS-01` — luật phiếu đặc tả engine).
> Plan sở hữu: [`253-gt000-concept-theme-plan.md`](../../../tasks/253-gt000-concept-theme-plan.md).
> Phương pháp sư phạm: bài học ba giai đoạn của Maria Montessori — Giới thiệu, Nhận biết, Gọi tên.
> Đơn vị gắn: **chủ đề**, neo vào các kỹ năng chơi thật — xem [`concept-topic-model.md`](../../05-content/concept-topic-model.md) mục 7.2.

## 1. Objective

`GT-000` là engine `kind = 'teach'` **duy nhất** trong kho. 36 engine còn lại đều **kiểm tra**
thứ trẻ đã biết; engine này **giới thiệu** thứ trẻ chưa biết. Đó là khác biệt quyết định.

Một level `GT-000` dạy trọn **một chủ đề** — dãy giá trị có thứ tự thuộc cùng một strand:
số 0–5, số 0–10, số 11–20, hình phẳng, màu cơ bản. Cấm — NEVER một level dạy đúng một giá trị
rồi để 10 giá trị còn lại của chủ đề không ai dạy.

Chủ đề dài hơn trí nhớ làm việc của trẻ ba tuổi, nên level chia thành **phân đoạn**. Mỗi phân
đoạn là một lát ba giai đoạn trọn vẹn trên 2–6 giá trị, có mốc nghỉ ở cuối. Level đi hết mọi
phân đoạn rồi đóng bằng một phân đoạn ôn gộp mọi giá trị đã dạy.

Trong một phân đoạn, mỗi giá trị đi qua đủ ba giai đoạn:

1. **Giai đoạn 1 — Giới thiệu.** "Đây là ..." Một giá trị hiện một mình, **được đọc lên**,
   không có gây nhiễu. Trẻ chạm để đi tiếp.
2. **Giai đoạn 2 — Nhận biết.** "Chỉ cho cô ..." Trẻ tìm đúng giá trị giữa các phương án
   phân tâm cùng nhóm tương phản.
3. **Giai đoạn 3 — Gọi tên.** "Đây là gì?" Trẻ nghe tên rồi chạm, củng cố khái niệm.

Level `GT-000` là **tiền đề**: nó gắn thẳng vào các kỹ năng chơi của một chủ đề, và cổng chặn
của mọi kỹ năng chơi cùng chủ đề. Chưa đi hết bài làm quen thì cổng chặn trước màn chơi.

## 2. Actors

| Actor               | Quyền cần          | Làm được gì ở đây                                                   |
| ------------------- | ------------------ | ------------------------------------------------------------------- |
| Trẻ 3–6             | —                  | Nghe từng giá trị, chạm chọn, đi hết chủ đề theo từng phân đoạn     |
| Người soạn nội dung | `content_reviewer` | Soạn `content_pack` chủ đề và `difficulty_params`                   |
| Bộ sinh level       | —                  | Chiếu dataset của chủ đề thành phân đoạn và dãy hành động           |
| Cổng kiểm thử       | —                  | Kiểm contract Zod, độ phủ chủ đề, và ca âm của từng luật `BR-E000-` |

## 3. Entry points

| Route / màn hình                                        | Actor     | Ghi chú                                              |
| ------------------------------------------------------- | --------- | ---------------------------------------------------- |
| `packages/game-engine/src/templates/GT-000/template.ts` | Dev       | Contract Zod `content_pack` và `difficulty_params`   |
| `packages/game-engine/src/templates/GT-000/session.ts`  | Dev       | Engine Session chạy phân đoạn và ba giai đoạn        |
| `packages/content/src/builders/gt-000.ts`               | Dev       | Bộ chiếu dataset chủ đề thành `content_pack`         |
| `docs/specs/01-platform/engines/GT-000.md`              | Mọi actor | Phiếu đặc tả này                                     |

## 4. Main flow

Bảy nhịp của [`engine-turn-script.md`](../engine-turn-script.md).

1. **`N1` Mở màn** — runtime nạp `content_pack`, đọc `segments` theo đúng thứ tự khai và mở
   phân đoạn trẻ đang dở (mốc lưu ở lần trước). Vẽ nền, rồi khay chất liệu của phân đoạn, rồi
   chất liệu đầu tiên. Nhận chạm **sau khi** hành động `present` đầu tiên phát xong tiếng.
2. **`N2` Ra đề** — engine `kind = 'teach'` nên "đề" là **lời giới thiệu**: `present` phát tên
   chất liệu một lần theo `auto_play_audio`; `recognise` và `recall` đọc câu hỏi một lần. Nút
   nghe lại đọc lại chính chất liệu đang nói tới. Kênh hình song song: chất liệu phóng to và
   đứng một mình trên màn.
   Lời đọc phát từ `instruction_audio_path` — một nguồn duy nhất (`BR-ETS-04`).
3. **`N3` Thao tác** — `tap` vào một chất liệu ở `recognise` · `recall`; `tap` hai đầu ở `link`.
   Ở `present` trẻ **không cần** thao tác nào: chạm chỉ để nghe lại.
4. **`N4` Phản hồi tức thì** — chạm đúng: chất liệu nảy nhẹ, phát tên nó lần nữa, khoá lại.
   Chạm sai ở `recognise`/`link`: cấm — NEVER báo sai; chạy lại `present` của chính mục tiêu
   rồi hỏi lại. Đây là điểm khác duy nhất của engine dạy so với 36 engine kiểm.
5. **`N5` Trợ giúp** — quá `hint_after_ms`: `L1` mục tiêu rung nhẹ và đọc lại lời dẫn một lần ·
   `L2` các phương án không phải mục tiêu mờ đi · `L3` chỉ còn mục tiêu sáng, giữ nguyên tới
   khi trẻ chạm. Cấm — NEVER tự đi tiếp thay trẻ.
6. **`N6` Kết lượt** — hết dãy hành động của một phân đoạn: mốc nghỉ, ăn mừng nhẹ, **ghi mốc**.
   Hết phân đoạn ôn cuối: `checkWinCondition()` trả `true`, phát `game_completed`. Bộ chất liệu
   vừa học ở lại trên màn trong nhịp mốc nghỉ.
7. **`N7` Chơi lại** — vào lại cùng level: **giữ mốc phân đoạn**, bắt đầu từ phân đoạn kế tiếp
   chưa học, không bắt học lại từ đầu. Thứ tự phương án phân tâm đổi theo seed mới; thứ tự
   phân đoạn và chất liệu **giữ nguyên** vì đó là trình tự dạy, không phải bàn chơi.

## 5. Alternative flows

Tám nhánh bắt buộc (`BR-ESS-19`), cộng nhánh riêng của engine.

| # | Nhánh | Điều kiện | Hành vi |
|---|---|---|---|
| 1 | Thao tác sai, còn lượt | Chạm sai ở `recognise`/`link` | Chạy lại `present` của mục tiêu rồi hỏi lại chính hành động đó |
| 2 | Thao tác sai, hết lượt | Chạm sai ở `recall` | Vẫn đi tiếp — kết quả là tín hiệu, không phải cửa |
| 3 | Trẻ dừng lại | Quá `hint_after_ms` | `L1`→`L3` như nhịp `N5`; sau `L3` giữ nguyên, cấm tự đi tiếp |
| 4 | Không nghe được lời đọc | Máy thiếu giọng `vi-VN` | Phát `tts_unavailable`, chạy tiếp bằng hình và mặt chữ; lượt vẫn hoàn thành được |
| 5 | Asset hỏng | `asset_id` của một hành động không có | Hoãn hành động đó, phát `intro_item_deferred`, phân đoạn vẫn chạy |
| 6 | Thiết bị yếu · `prefers-reduced-motion` | FPS tụt | Bỏ hạt lớp 4; phóng to chất liệu ở `present` cấm — NEVER bỏ |
| 7 | Bỏ dở giữa lượt | Thoát sau phân đoạn `k` | Phiên `abandoned`; lần sau bắt đầu từ phân đoạn `k + 1` |
| 8 | Chơi lại lần thứ n | Trẻ mở lại level đã học xong | Chạy phân đoạn ôn, không chạy lại toàn bộ chuỗi dạy |

## 6. Business rules

| ID           | Rule                                                                                                                                                                                 | Vì sao                                                                                                                                                                                    |
| ------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `BR-E000-01` | Template mang `kind = 'teach'` và Cấm — NEVER ghi điểm mastery khi phiên đóng                                                                                                          | Bài làm quen dạy, không chấm. `BR-CIR-11` (luật runner cấm ghi mastery) đã chốt điều này ở tầng runner; ở đây là điều kiện của chính khuôn                                                     |
| `BR-E000-02` | Mọi chất liệu BẮT BUỘC có một hình minh hoạ và một nhãn tiếng Việt đọc lên được. Trường hình Cấm — NEVER để tuỳ chọn trong schema                                                        | Trẻ chưa đọc chữ nhận khái niệm qua tai và mắt. Chất liệu không hình hoặc không tên là chất liệu không dạy được. Để tuỳ chọn thì cổng không bắt được, và hiện tại nó đang tuỳ chọn                |
| `BR-E000-03` | Một `segment` có **2–3** chất liệu và **3–12** hành động. Một level có **1–8** phân đoạn và tối đa **21** chất liệu phân biệt                                                            | Trần chất liệu là trí nhớ làm việc của trẻ ba tuổi trong một mạch liền, giữ nguyên lý do của `BR-CIM-03` (trần biên tập bài làm quen). Từ 2026-09-06 mỗi chất liệu tốn bốn hành động vì có thêm `echo` (`BR-E000-10`), nên trần chất liệu là **3** chứ không phải 6 — 4 chất liệu là 16 hành động, vượt trần 12. Trần 21 là dãy dài nhất một chủ đề cần — số 0 tới 20 |
| `BR-E000-04` | Mọi phân đoạn BẮT BUỘC kết thúc bằng ít nhất một hành động `recall`. Level BẮT BUỘC đóng bằng một phân đoạn ôn gộp mọi giá trị đã dạy                                                    | `recall` là phép đo xếp chỗ của phân đoạn. Không có phân đoạn ôn thì trẻ học sáu giá trị rời rạc, không bao giờ thấy chúng đứng cạnh nhau — mà chủ đề chính là chỗ chúng đứng cạnh nhau         |
| `BR-E000-05` | Mỗi hành động `present` BẮT BUỘC phát tiếng trước khi nhận chạm: `audio_path` nếu có, không thì bộ đọc `vi-VN`. Không có giọng thì phát `tts_unavailable` và chạy tiếp bằng hình và chữ | Kênh duy nhất trẻ chưa đọc chữ nhận được là kênh tai. Hiện `narration_line` chỉ được vẽ ra chữ, nên bài làm quen đang câm — đó đúng là lỗi mà engine này sinh ra để sửa                        |
| `BR-E000-06` | Khuôn BẮT BUỘC khai `input` họ `tap`, và Session BẮT BUỘC có `getView()` cùng `toAction()`                                                                                              | Thiếu ba thứ này thì trang chơi không dựng được cử chỉ, `dispatch()` trả về bỏ qua, và trẻ chạm vào màn hình không có gì xảy ra. Đây là trạng thái thật của khuôn hôm nay                       |
| `BR-E000-07` | Mọi tên sự kiện Session phát BẮT BUỘC nằm trong danh mục sự kiện chung. Cấm — NEVER phát tên chưa đăng ký                                                                              | Tầng nhận telemetry ném cả lô khi gặp một tên lạ, nên một tên chưa đăng ký làm hỏng luôn bước đóng phiên và phiên treo dở                                                                      |
| `BR-E000-08` | `tts_used` trong payload sự kiện BẮT BUỘC là kết quả thật của lần phát tiếng. Cấm — NEVER là hằng số                                                                                     | Hằng số `true` biến telemetry thành lời khai. Số liệu phủ audio đọc từ trường này, nên nó sai thì mọi kết luận về kênh tai đều sai                                                             |
| `BR-E000-09` | Level `GT-000` BẮT BUỘC gắn **mọi** kỹ năng chơi mà chủ đề đó dạy, đúng bằng `concept.teaches[]`                                                                                        | Hai cổng đọc hai chỗ khác nhau: cổng phủ so khớp mã kỹ năng đúng bằng, còn cổng chặn tra bảng ánh xạ nội dung. Gắn thiếu một mã thì kỹ năng đó vẫn mở thẳng và nợ độ phủ không giảm             |
| `BR-E000-10` | Mọi phân đoạn **không** phải phân đoạn ôn BẮT BUỘC có ít nhất một hành động `echo`. Hành động `echo` phát mẫu rồi chờ trẻ chạm; Cấm — NEVER yêu cầu micro, Cấm — NEVER chấm phát âm       | `recall` dạy trẻ **nhận ra** một cái tên; `echo` dạy trẻ **gọi** được nó. Hai việc khác nhau và corpus hiện chỉ có việc thứ nhất. Máy không cần nghe để trẻ tập nói — giá trị nằm ở việc trẻ phát ra tiếng |

## 7. Data

`content_pack` có ba khối: khối `concept` khai chủ đề, kỹ năng neo, danh sách kỹ năng nó dạy
và số thứ tự tiết; khối
`assets` là kho chất liệu của cả level; khối `segments` là các phân đoạn, mỗi phân đoạn có
danh sách chất liệu của mình và dãy hành động trên chúng. Mọi hành động trỏ chất liệu bằng mã
chất liệu, Cấm — NEVER nhúng nhãn, hình hay đường dẫn audio thẳng vào một hành động.

`difficulty_params` khai `hint_after_ms`, `allow_retry`, `auto_play_audio`. Nó khai **nhịp**,
Cấm — NEVER khai nội dung.

Contract đầy đủ nằm ở schema trong `template.ts` — cấm — NEVER chép lại (`BR-ESS-03`).
Ràng buộc biên tập của bài làm quen ở [`concept-intro-model.md`](../../05-content/concept-intro-model.md) mục 6;
từ vựng chủ đề ở [`concept-topic-model.md`](../../05-content/concept-topic-model.md) mục 7.2.

## 8. API contract

Không có. Engine chạy trong tiến trình frontend/canvas runtime.

## 9. Acceptance criteria

```gherkin
Scenario: BR-E000-01 — Hoàn thành bài làm quen không cập nhật mastery
  Given Trẻ đi hết mọi phân đoạn của một level GT-000
  When Phiên chơi gửi yêu cầu session-complete lên máy chủ
  Then Trạng thái hoàn thành được ghi trong play_sessions
  And mastery_state không đổi điểm số

Scenario: BR-E000-02 — Chất liệu thiếu hình hoặc thiếu nhãn thì không hợp lệ
  Given Người soạn khai một chất liệu không có hình minh hoạ
  When Hệ thống kiểm content_pack qua schema
  Then Schema từ chối
  And lý do nêu chất liệu phải có hình và nhãn tiếng Việt

Scenario: BR-E000-03 — Phân đoạn bảy chất liệu thì bị từ chối
  Given Một level GT-000 có một phân đoạn khai 7 chất liệu
  When Hệ thống kiểm content_pack qua schema
  Then Schema từ chối
  And lý do nêu trần chất liệu mỗi phân đoạn là 6

Scenario: BR-E000-04 — Phân đoạn không có recall thì bị từ chối
  Given Một level GT-000 có một phân đoạn chỉ gồm present và recognise
  When Hệ thống kiểm content_pack qua schema
  Then Schema từ chối
  And lý do nêu mỗi phân đoạn phải kết bằng ít nhất một recall

Scenario: BR-E000-05 — Bước giới thiệu phải phát tiếng trước khi nhận chạm
  Given Một hành động present của một chất liệu có nhãn tiếng Việt
  When Runner mở hành động đó
  Then Runner phát audio_path nếu có, không thì gọi bộ đọc vi-VN
  And Runner chỉ nhận chạm sau khi lệnh phát tiếng đã được gọi

Scenario: BR-E000-06 — Trẻ chạm vào chất liệu thì bước tiến lên
  Given Một phiên GT-000 đang mở một hành động present
  When Trẻ chạm vào chất liệu đang hiện
  Then Session nhận cử chỉ qua toAction và chuyển sang hành động kế tiếp

Scenario: BR-E000-07 — Tên sự kiện chưa đăng ký thì cổng chặn
  Given Session GT-000 khai một tên sự kiện không có trong danh mục chung
  When Cổng danh mục sự kiện chạy
  Then Cổng báo lỗi và nêu đúng tên sự kiện chưa đăng ký

Scenario: BR-E000-08 — tts_used phản ánh lần phát thật
  Given Máy của trẻ không có giọng vi-VN và chất liệu không có audio_path
  When Runner mở một hành động present
  Then Sự kiện phát ra mang tts_used bằng false
  And Runner phát thêm sự kiện tts_unavailable

Scenario: BR-E000-09 — Level làm quen gắn thiếu kỹ năng thì cổng chặn
  Given Một level GT-000 chỉ gắn kỹ năng bậc pre mà không gắn kỹ năng chủ đề dạy
  When Cổng phủ bài làm quen chạy
  Then Cổng báo lỗi và nêu các mã kỹ năng còn thiếu

Scenario: BR-E000-10 — Phân đoạn dạy thiếu hành động echo thì bị từ chối
  Given Một phân đoạn không phải phân đoạn ôn của level GT-000
  When Phân đoạn không khai bất kỳ hành động echo nào
  Then Schema từ chối và yêu cầu mỗi phân đoạn dạy có ít nhất một hành động echo
```

## 10. Boundaries

**Always**

- Lấy màu và font hoàn toàn từ `systems/designTokens.ts`.
- Tính kích thước vùng chạm qua `computeAdaptiveRadius(ageBand)`.
- Giới thiệu một chất liệu trước khi hỏi nó, trong cùng phân đoạn hoặc phân đoạn trước.
- Ghi mốc cuối mỗi phân đoạn để trẻ quay lại đúng chỗ.

**Ask first**

- Thêm hành động tương tác thứ năm ngoài `present`, `recognise`, `link`, `recall`.
- Nới trần 21 chất liệu mỗi level.

**Never**

- Cấm — NEVER hiện màu đỏ trừng phạt hoặc âm báo sai khi trẻ chọn sai.
- Cấm — NEVER gán `kind = 'assess'` cho `GT-000`.
- Cấm — NEVER hỏi một chất liệu chưa từng được giới thiệu.
- Cấm — NEVER xin quyền micro hoặc lưu giọng trẻ.
- Cấm — NEVER đặt điểm, sao hay đồng hồ đếm ngược vào bài làm quen.

## 11. Open questions

| #   | Câu hỏi                                                                        | Chặn gì                                       | Chặn phase | Chủ         |
| --- | ------------------------------------------------------------------------------ | --------------------------------------------- | ---------- | ----------- |
| 1   | Giọng đọc dùng kho mp3 mượn của v1 hay sinh mới toàn bộ cho mọi nhãn chủ đề?   | Chất lượng âm thanh trên thiết bị giá rẻ      | P4         | Nội dung    |
| 2   | Phân đoạn ôn cuối có được phép dùng chất liệu ngoài level làm phương án không? | Độ khó của phân đoạn ôn ở chủ đề 11 giá trị   | P4         | Nội dung    |

## 12. Hợp đồng vẽ

Hợp đồng chung: [`engine-render-contract.md`](../engine-render-contract.md).

**Slot dùng:** `single-focus`, `grid`, `horizontal-row`, `flex-wrap`

| Lớp                  | Engine này vẽ gì                                                                       |
| -------------------- | -------------------------------------------------------------------------------------- |
| 1. Nền cảnh          | Nền phẳng theo token màu dịu                                                            |
| 2. Phần tử tĩnh      | Lời dẫn của hành động, nhãn chủ đề, dải mốc phân đoạn                                   |
| 3. Phần tử tương tác | Thẻ chất liệu, phương án chọn, mục tiêu nối, nút "Bé nói theo" và nút nghe lại          |
| 4. Lớp phản hồi      | Vòng sáng hổ phách khi nhắc lại, pháo hoa nhẹ ở mốc cuối phân đoạn và khi đóng level    |

**Trạng thái thị giác riêng:** Thẻ đang được đọc ở giai đoạn 1 nổi bật hơn mọi thẻ khác, và dải
mốc phân đoạn tô đầy dần theo số phân đoạn đã xong. Ở hành động `echo`, thẻ mục tiêu đứng một
mình giữa màn, có một nhịp sóng âm chạy trong lúc phát mẫu rồi dừng — dấu hiệu thị giác cho
"tới lượt bé nói", vì trẻ chưa đọc được chữ hướng dẫn.
**Thứ tự tuột khi thiết bị yếu:** Lớp 4 (hạt) -> Lớp 2 (chữ phụ). Lớp 3 cấm — NEVER bỏ.

## 13. Ma trận seed mục tiêu

| Band  | `observe` | `recall` | Tổng mục tiêu |
| ----- | :-------: | :------: | :-----------: |
| `3-4` |    ≥2     |    ≥2    |      ≥4       |
| `4-5` |    ≥1     |    ≥1    |      ≥2       |
| `5-6` |    ≥1     |    ≥1    |      ≥2       |

Mỗi level BẮT BUỘC có ≥1 hành động `echo` trên mỗi phân đoạn dạy — `BR-E000-10`.

Trục `what` mục tiêu: `number`, `geometry`, `colour` (≥3 giá trị khác nhau).
Trục `theme` mục tiêu: `farm`, `school`, `home` (≥3 giá trị khác nhau).

## 14. Ca sai không bắt được bằng schema

- Một level khai đủ sáu phân đoạn và mọi phân đoạn đều có `recall`, nhưng phân đoạn ôn cuối chỉ
  ôn ba giá trị của phân đoạn ngay trước nó. Schema đếm được số phân đoạn và số hành động,
  không đếm được **giá trị nào đã được dạy mà không được ôn**. Trẻ đi hết bài mà chưa lần nào
  thấy 11 giá trị của chủ đề đứng cạnh nhau — mà đó chính là thứ chủ đề dạy.
- Một level chia phân đoạn theo đúng trần nhưng cắt giữa một cặp tương phản: `6` ở cuối phân
  đoạn hai, `9` ở đầu phân đoạn ba. Hai chữ số dễ lẫn nhất không bao giờ xuất hiện cùng một
  màn, nên bài không bao giờ dạy trẻ phân biệt chúng.
- Mọi `narration_line` đều được khai và schema xanh, nhưng người soạn viết chúng bằng văn viết
  chứ không phải lời nói. Đọc lên nghe như đọc tài liệu, và trẻ ba tuổi rời đi trước phân đoạn hai.
- Một level có đủ hành động `echo` ở mọi phân đoạn, nhưng mẫu đọc là một câu dài — "năm quả táo
  thì nhiều hơn ba quả táo" — dài quá sức nhắc lại của trẻ ba tuổi. Schema đếm được số hành
  động, không đếm được **số âm tiết trẻ phải giữ trong đầu**. Trẻ im lặng bấm qua, và bước tập
  nói thành bước bấm nút.
- Level dạy khai `concept.teaches[]` đủ tám mã, nhưng `skill_codes` của header chỉ có hai. Cả
  hai trường đều hợp lệ theo schema riêng của chúng; chỉ phép so **giữa** hai trường mới bắt
  được, và đó là `BR-E000-09`.

## 15. Trường trích từ registry

Trích từ [`GT-000/template.ts`](../../../../packages/game-engine/src/templates/GT-000/template.ts).

| Trường                  | Giá trị                                                     |
| ----------------------- | ----------------------------------------------------------- |
| `mechanic`              | `concept-intro`                                             |
| `layouts`               | `single-focus`, `grid`, `horizontal-row`, `flex-wrap`       |
| `age_min` · `age_max`   | `3` · `6`                                                   |
| `banned_age_bands`      | `[]`                                                        |
| `requires_tap_fallback` | `false`                                                     |
| `limits`                | `item_count` [2, 6] · `distractor_count` [1, 3] · `target_count` [1, 1] |
| `asset_kinds`           | `["emoji", "image", "audio"]`                               |
| `engine_session`        | `GT000Session`                                              |
| `input`                 | `family: "tap"`, `verbs: ["tap"]`, `tolerance_px: 24`       |

Trường `input` đã được khai báo theo `BR-E000-06` (Task #253).

## 16. Chiều sâu nội dung

Sáu số đo hiện tại và mục tiêu bậc 1 (`BR-ECD-01`…`-06` — luật chiều sâu nội dung engine):

- `level_count`: hiện có 9 (4 level nền + 5 level chủ đề bậc pre), mục tiêu ≥6 (ĐẠT)
- `min_band_count`: hiện có đủ 3 band (`3-4`, `4-5`, `5-6`), mục tiêu ≥1 (ĐẠT)
- `thinking_span`: hiện có 4 (`count`, `observe`, `recall`, `describe`), mục tiêu ≥2 (ĐẠT)
- `what_span`: hiện có 4 (`number`, `letter`, `geometry`, `detect`), mục tiêu ≥2 (ĐẠT)
- `theme_span`: hiện có 5 (`farm`, `school`, `ocean`, `space`, `nature`), mục tiêu ≥2 (ĐẠT)
- `access_tier`: ≥1 level `free` hoặc `login` (ĐẠT)

## 17. Miền hành vi

Từ vựng và luật: [`engine-behavior-domain.md`](../engine-behavior-domain.md).

**Miền chủ đạo:** `chi-dinh` · **Miền phụ:** — · **Cử chỉ chủ đạo:** `tap` ·
**Ràng buộc nhịp:** `tự do`

Đây là engine duy nhất mà **một giai đoạn không đòi hành vi nào**: ở giai đoạn giới thiệu trẻ
chỉ nghe và nhìn. Hành vi bắt đầu ở giai đoạn hai (chỉ theo tên) và đạt đỉnh ở giai đoạn ba
(gọi tên) — mà giai đoạn ba là hành vi **nói**, thứ nền tảng không quan sát được vì không chạm
micro. Xem câu hỏi 3 mục 11 của [`engine-behavior-domain.md`](../engine-behavior-domain.md).

| Câu quan sát được | Band | Là bằng chứng của |
|---|:--:|---|
| "Con nhìn theo vật đang được giới thiệu suốt cả phân đoạn, không quay đi" | `3-4` | Chú ý chung — điều kiện của mọi việc học sau đó |
| "Con chỉ đúng con vịt khi nghe tên nó, chưa cần nói được tên" | `3-4` | Vốn từ tiếp nhận đi trước vốn từ diễn đạt |
| "Con gọi tên vật khi được hỏi cái này là gì" | `4-5` | Vốn từ diễn đạt — **hệ thống không ghi nhận được**, người lớn ghi |
| "Con nhắc lại tên vật của phân đoạn trước khi sang phân đoạn ôn" | `4-5` | Trí nhớ dài hạn của chủ đề, không chỉ của lượt chơi |

**Điều kiện phát triển tiên quyết** (`BR-EBD-06`)

| Điều kiện | Ngưỡng band nhỏ nhất | Hệ quả thiết kế |
|---|:--:|---|
| Chú ý | `3-4` | Phân đoạn 2–6 giá trị, có mốc nghỉ cuối mỗi phân đoạn |
| Ngôn ngữ tiếp nhận | `3-4` | Câu giới thiệu một mệnh đề, giọng Việt thật, nghe lại được |
| Vận động | `3-4` | Một cú chạm, không kéo — engine rào cản thấp nhất cùng `GT-001` |
| Bộ nhớ làm việc | `3-4` | Giai đoạn ôn chỉ gộp các giá trị **đã** dạy trong cùng level |

**Bậc biểu diễn theo band** (`BR-EBD-12`): `3-4` vật và hình · `4-5` hình, ký hiệu luôn đi
kèm lượng · `5-6` ký hiệu đứng riêng ở phân đoạn ôn.

**Kênh thắng cuộc** (`BR-EBD-07`): `hình` · `âm` · `ký hiệu` — thẻ chất liệu vẽ ra vật, nút nghe lại và nút "Bé nói theo" mang lời dẫn, phân đoạn ôn dùng ký hiệu

## 18. Trục biến thể và độ mở

| Trục | Giá trị dùng ở engine này | Có trong ma trận mục 13 |
|---|---|:--:|
| `boi-canh` | `farm` · `school` · `ocean` · `space` · `nature` | Có |
| `vai-tro` | người nghe (giai đoạn 1) · người chỉ (giai đoạn 2) · người gọi tên (giai đoạn 3) | Chưa |
| `kenh-de` | giọng Việt thật · hình · ký hiệu | Chưa |
| `do-mo` | không có đáp án sai ở giai đoạn 1 · một vật đúng ở giai đoạn 2 | Chưa |

**Độ mở (`do_mo`):** `bán mở` — giai đoạn giới thiệu **không có đáp án sai**, đó là điều làm
`kind = 'teach'` khác 36 engine còn lại; giai đoạn kiểm lại chỉ nhận một vật đúng.
**Quyền của trẻ (`BR-EBD-09`):** nghe lại phần giới thiệu bao nhiêu lần tuỳ ý; quyết định lúc
sang phân đoạn kế tiếp; dừng ở mốc nghỉ mà không mất tiến độ.
