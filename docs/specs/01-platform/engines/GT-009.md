---
spec: ENGINE-GT009
title: Loại trừ theo manh mối — clue-deduction
area: platform
status: approved
mvp: true
phase: P4
reviewed: 2026-08-30
engine: clue-deduction
batch: montessori
owns:
  - Hợp đồng nội dung của engine GT-009
  - Hợp đồng vẽ của engine GT-009
  - Ma trận seed mục tiêu của engine GT-009
depends_on:
  - GAME-TEMPLATE-CONTRACT
  - GAME-LAYOUT-ENGINE
  - ENGINE-RENDER-CONTRACT
  - ENGINE-SPEC-SHEET
---

# GT-009 — Loại trừ theo manh mối

## 1. Objective

Trẻ nghe hai đến bốn manh mối, loại dần ứng viên, và chỉ ra cái còn lại. Đây là engine duy
nhất trong corpus dạy **loại trừ** như một thao tác: không phải "cái nào đúng" mà "cái nào
không thể sai". Nó là nền của C6 suy luận.

## 2. Actors

| Actor | Quyền cần | Làm được gì ở đây |
|---|---|---|
| Trẻ 4–6 | — | Tương tác với engine clue-deduction |
| Người soạn nội dung | `content_reviewer` | Soạn `content_pack` và `difficulty_params` |
| Cổng | — | Kiểm tra tính hợp lệ |

## 3. Entry points

| Route / màn hình | Actor | Ghi chú |
|---|---|---|
| `packages/game-engine/src/templates/GT-009/template.ts` | Dev | Định nghĩa contract |
| `packages/game-engine/src/templates/GT-009/session.ts` | Dev | Logic + render |
| `docs/specs/01-platform/engines/GT-009.md` | Dev | Spec này |

## 4. Main flow

1. Vòng lặp nạp `content_pack` đã parse bằng schema.
2. `setupEntities()` khởi tạo trạng thái.
3. Trẻ tương tác theo cơ chế `clue-deduction`.
4. `validateAction` kiểm tra hành động.
5. Phản hồi thị giác theo trạng thái.
6. `checkWinCondition()` trả `true` khi hoàn thành — thuần, không side effect.

## 5. Alternative flows

| Nhánh | Điều kiện | Hành vi |
|---|---|---|
| Sai | Trẻ thao tác sai | Phản hồi amber nudge, phiên vẫn chạy |
| Hết giờ gợi ý | Timeout | Scaffolding highlight |
| Asset hỏng | Emoji/image fail | Vẽ ô giữ chỗ |
| Thiết bị yếu | FPS tụt | Bỏ hạt lớp 4, giữ lớp 3 |

## 6. Business rules

| ID | Rule | Vì sao |
|---|---|---|
| `BR-E009-01` | Band hợp lệ: `3-4`, `4-5`, `5-6` | Phù hợp phát triển vận động và nhận thức của lứa tuổi |
| `BR-E009-02` | `checkWinCondition()` thuần — không side effect | Tính toán không được ảnh hưởng trạng thái |

## 7. Data

Contract đầy đủ nằm ở schema trong `template.ts` — cấm chép lại schema (`BR-ESS-03`).

## 8. API contract

Không có. Engine chạy trong tiến trình frontend/canvas runtime.

## 9. Acceptance criteria

```gherkin
Scenario: GT-009 — đúng một ứng viên thoả mọi manh mối
  Given một content_pack với 6 candidates và 3 clues
  When áp mọi clue lên candidates
  Then đúng một candidate còn lại
  And nó bằng answer_candidate_id

Scenario: GT-009 — manh mối đã dùng vẫn đọc lại được
  Given trẻ đã nghe manh mối 1
  When trẻ chạm vào manh mối 1
  Then manh mối phát lại
  And không tính là một lần sai

Scenario: BR-E009-01 — band hợp lệ
  Given một level GT-009
  When band nằm ngoài phạm vi cho phép
  Then cổng validate đỏ

Scenario: BR-E009-02 — checkWinCondition thuần
  Given một phiên GT-009
  When gọi checkWinCondition 100 lần
  Then trạng thái không đổi
```

## 10. Boundaries

**Always** — mỗi manh mối loại ít nhất một ứng viên; đáp án duy nhất sau khi áp đủ manh mối.

**Ask first** — `clue_count` 4 ở band 4-5; manh mối phủ định ở band 5-6.

**Never** — manh mối thừa; manh mối phủ định ở band 4-5; hai ứng viên cùng thoả.

## 11. Open questions

| # | Câu hỏi | Chặn gì | Chặn phase | Chủ |
|---|---|---|---|---|
| 1 | Engine ở mức mẫu 3 level. Nó phủ `infer` — một trong ba giá trị trục tư duy đang dưới sàn ở [`thinking-coverage-matrix.md`](../../08-quality/thinking-coverage-matrix.md). Có nên ưu tiên engine này trong lô nội dung đầu tiên không? | Thứ tự lô nội dung | P4 | Nội dung |

## 12. Hợp đồng vẽ

Hợp đồng chung, gồm bốn lớp và năm trạng thái thị giác:
[`engine-render-contract.md`](../engine-render-contract.md).

**Slot dùng:** `clue-board`; slot `neutral` cho ứng viên, vùng manh mối tách riêng.

| Lớp | Engine này vẽ gì |
|---|---|
| 1. Nền cảnh | Bảng manh mối ở một bên, lưới ứng viên ở bên kia |
| 2. Phần tử tĩnh | Manh mối đã nghe vẽ dạng thẻ, chạm để nghe lại |
| 3. Phần tử tương tác | 4–10 ứng viên; ứng viên bị loại giảm nhấn nhưng vẫn hiện |
| 4. Lớp phản hồi | Ứng viên đúng nở nhẹ kèm hạt |

Trạng thái riêng: **đã loại** — mờ đi cộng dấu gạch, cấm xoá khỏi màn hình. Trẻ cần thấy mình đã loại trừ được bao nhiêu.

**Thứ tự tuột khi thiết bị yếu:** theo mục 7.4 của hợp đồng chung. Lớp 3 và vùng chạm của nó
cấm — NEVER bỏ.

## 13. Ma trận seed mục tiêu

| Band | `infer` | `observe` | `recall` | `plan` | Tổng mục tiêu |
|---|:--:|:--:|:--:|:--:|:--:|
| `4-5` | ≥3 | ≥2 | ≥1 | — | ≥6 |
| `5-6` | ≥3 | ≥1 | ≥1 | ≥1 | ≥6 |

Trục `what` mục tiêu: `category` · `colour` · `size` · `space`.
Hiện có 3 level, **1 giá trị `thinking`, 1 chủ đề, 1 competency** — mức mẫu, thủng bậc 1.

## 14. Ca sai không bắt được bằng schema

Bốn manh mối mà chỉ ba manh mối đầu đã đủ xác định đáp án. Parse sạch; sai vì manh mối thừa
dạy trẻ rằng đọc hết là vô ích. Mỗi manh mối phải **loại ít nhất một** ứng viên còn lại.

Ca thứ hai: manh mối phủ định ở band 4-5 ("không phải màu đỏ"). Phủ định là bước nhận thức
riêng và thuộc band 5-6.

## 15. Trường trích từ registry

Trích từ [`GT-009/template.ts`](../../../../packages/game-engine/src/templates/GT-009/template.ts).

| Trường | Giá trị |
|---|---|
| `mechanic` | `clue-deduction` |
| `layouts` | `clue-board` |
| `age_min` · `age_max` | 4 · 6 |
| `banned_age_bands` | — |
| `requires_tap_fallback` | `false` |
| `asset_kinds` | `emoji` · `image` · `audio` |
| `engine_session` | `ClueDeductionSession` |

## 16. Chiều sâu nội dung

- `level_count`: cần đo, mục tiêu ≥6
- `min_band_count`: cần đo, mục tiêu ≥1
- `thinking_span`: cần đo, mục tiêu ≥2
- `what_span`: cần đo, mục tiêu ≥2
- `theme_span`: cần đo, mục tiêu ≥2
- `access_tier`: cần đo, ≥1 level `free` hoặc `login`
