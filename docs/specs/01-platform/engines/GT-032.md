---
spec: ENGINE-GT032
title: So lượng chất lỏng — pour-quantity
area: platform
status: draft
mvp: false
phase: P4
reviewed: 2026-08-31
engine: pour-quantity
batch: legacy-v1
owns:
  - Hợp đồng nội dung của engine GT-032
  - Hợp đồng vẽ của engine GT-032
  - Ma trận seed mục tiêu của engine GT-032
depends_on:
  - GAME-TEMPLATE-CONTRACT
  - GAME-LAYOUT-ENGINE
  - ENGINE-RENDER-CONTRACT
  - ENGINE-SPEC-SHEET
---

# GT-032 — So lượng chất lỏng

> Phiếu **đặt trước** (`BR-ESS-15`). Plan sở hữu:
> [`185-engine-gt-032-pour-quantity-plan.md`](../../../tasks/185-engine-gt-032-pour-quantity-plan.md).
> Nguồn v1 gánh: `legacy_v1_ref: "D5-09"` (Nhiều/Ít chất lỏng).

## 1. Objective

Trẻ so lượng nước giữa các cốc **khác hình dạng** rồi trả lời cốc nào nhiều hơn, ít hơn, hay bằng nhau.

Thứ duy nhất đáng giữ của bài này là **bẫy bảo toàn lượng của Piaget**: cùng một lượng nước, rót
sang cốc cao hẹp thì trẻ 4–5 tuổi nói là nhiều hơn. Một bài chọn đáp án thường không dựng được bẫy
đó, vì bẫy nằm ở tương quan giữa hình dạng cốc và mức nước, không nằm ở câu hỏi.

**Lượng là rời rạc, không liên tục** (quyết định 2026-08-31): `fill_units` là số nguyên trong
`[0, capacity_units]`. Nguồn v1 vốn đã rời rạc, nên mở một kiểu liên tục là thêm bề mặt mà không
thêm giá trị sư phạm — và bộ sinh mất luôn khả năng kiểm được bẫy.

## 2. Actors

| Actor | Quyền cần | Làm được gì ở đây |
|---|---|---|
| Trẻ 5–6 | — | So các cốc rồi chọn đáp án |
| Người soạn nội dung | `content_reviewer` | Soạn `content_pack` và `difficulty_params` |
| Bộ sinh level | — | Sinh level và **kiểm được** bẫy bảo toàn nhờ mức rời rạc |
| Cổng | — | Kiểm seed, contract và hợp đồng vẽ |

## 3. Entry points

| Route / màn hình | Actor | Ghi chú |
|---|---|---|
| `packages/game-engine/src/templates/GT-032/template.ts` | Dev | Contract — task #185 dựng |
| `packages/game-engine/src/templates/GT-032/session.ts` | Dev | Dựng trên nguyên thuỷ `selection` |
| `packages/game-engine/src/generators/gt032.ts` | Bộ sinh | ≥8 chủ đề |
| `docs/specs/01-platform/engines/GT-032.md` | mọi actor | Phiếu này |

## 4. Main flow

1. Vòng lặp nạp `content_pack` đã parse.
2. `setupEntities()` vẽ 2–4 cốc theo `shape`, mức nước theo `fill_units` trên `capacity_units`.
3. Trẻ đọc câu hỏi theo `question_type` (`more` · `less` · `same` · `pour_to_mark`).
4. Trẻ chạm cốc trả lời, hoặc kéo mức nước tới vạch nếu `question_type` là `pour_to_mark`.
5. `checkWinCondition()` so đáp án với `fill_units` thật — thuần, không side effect.

## 5. Alternative flows

| Nhánh | Điều kiện | Hành vi |
|---|---|---|
| Chọn sai vì bẫy bảo toàn | `conservation_trap` bật | Amber nudge cộng gợi ý đếm vạch mức, không nói thẳng đáp án |
| Chọn sai đáp án | `allow_retry` bật | Nudge, cốc giữ nguyên |
| Hết giờ gợi ý | Quá `hint_after_ms` | Scaffolding hiện vạch chia đơn vị trên thành cốc |
| Asset hỏng | Ảnh cốc fail | Vẽ cốc bằng nguyên thuỷ hình học — hình dạng là nội dung, cấm — NEVER thay bằng ô vuông chung |
| Thiết bị yếu | FPS tụt | Bỏ gợn sóng mặt nước; mức nước và tỉ lệ cốc cấm — NEVER đổi |

## 6. Business rules

| ID | Rule | Vì sao |
|---|---|---|
| `BR-E032-01` | `fill_units` là số nguyên trong `[0, capacity_units]` với mọi cốc | Mức rời rạc là điều kiện để bộ sinh và cổng kiểm được bẫy bảo toàn; số thực thì không kiểm được |
| `BR-E032-02` | Khi `conservation_trap` bật, phải có ≥2 cốc **cùng** `fill_units` mà **khác** `shape` | Không có cặp đó thì cờ bật mà bẫy không tồn tại — một trường nói dối |
| `BR-E032-03` | Chiều cao cột nước vẽ ra phải tỉ lệ với `fill_units / capacity_units` của **chính cốc đó** | Vẽ sai tỉ lệ biến bẫy bảo toàn thành lỗi hiển thị, và trẻ trả lời đúng vì lý do sai |
| `BR-E032-04` | Band cấm: `3-4` · `4-5` | Vượt bẫy bảo toàn là mốc phát triển của lứa 5–6; đặt sớm hơn là đo thất bại |

## 7. Data

**`content_pack`:** `prompt` · `prompt_audio_ref` · `cups[]` (2–4, mỗi phần tử `cup_id` + `shape` +
`capacity_units` + `fill_units`) · `question_type` (`more` \| `less` \| `same` \| `pour_to_mark`) ·
`conservation_trap`.
**`difficulty_params`:** `cup_count` · `level_steps` · `conservation_trap` · `allow_retry` ·
`hint_after_ms`.
**Band hợp lệ:** `5-6`. **Limits đặt trước:** mục 15.

Contract đầy đủ nằm ở schema trong `template.ts` — cấm — NEVER chép lại (`BR-ESS-03`).

## 8. API contract

Không có. Engine chạy trong tiến trình frontend/canvas runtime.

## 9. Acceptance criteria

```gherkin
Scenario: BR-E032-01 — mức nước là số thực làm contract đỏ
  Given một cup khai fill_units là 2,5
  When parse content_pack qua contract của GT-032
  Then refine từ chối vì fill_units phải là số nguyên

Scenario: BR-E032-02 — bật bẫy bảo toàn mà không có cặp cốc hợp lệ làm contract đỏ
  Given conservation_trap bật
  And ba cốc đều khác fill_units
  When parse content_pack qua contract của GT-032
  Then refine từ chối vì thiếu cặp cùng mức khác hình

Scenario: BR-E032-03 — cột nước vẽ đúng tỉ lệ của chính cốc đó
  Given cốc A có capacity_units 4 và fill_units 2
  And cốc B có capacity_units 8 và fill_units 2
  When engine dựng hình hai cốc
  Then cột nước cốc A cao một nửa thân cốc A
  And cột nước cốc B cao một phần tư thân cốc B

Scenario: BR-E032-04 — band 4-5 làm cổng seed đỏ
  Given một level GT-032 gắn band 4-5
  When chạy cổng kiểm seed
  Then cổng thoát với mã khác 0
```

## 10. Boundaries

**Always**
- Giữ mức nước ở đơn vị nguyên.
- Vẽ vạch chia đơn vị khi trẻ xin gợi ý.
- Dựng bẫy bảo toàn bằng cặp cốc cùng mức khác hình.

**Ask first**
- `cups` vượt 4 cốc.
- Thêm `question_type` ngoài bốn giá trị đang khai.

**Never**
- Dùng số thực cho lượng (`BR-E032-01`).
- Vẽ mọi cốc cùng một hình khi `conservation_trap` bật.
- Dùng engine này dưới band `5-6`.

## 11. Open questions

| # | Câu hỏi | Chặn gì | Chặn phase | Chủ |
|---|---|---|---|---|
| 1 | Vạch chia đơn vị có nên hiện thường trực ở mức khó thấp không? Hiện thì bài dễ đúng nhưng bẫy mất tác dụng | Trục khó của bộ sinh | P4 | Nội dung |

## 12. Hợp đồng vẽ

Hợp đồng chung: [`engine-render-contract.md`](../engine-render-contract.md).

**Slot dùng:** `horizontal-row` · `split-columns`

| Lớp | Engine này vẽ gì |
|---|---|
| 1. Nền cảnh | Nền phẳng, mặt bàn đặt cốc thẳng hàng |
| 2. Phần tử tĩnh | Câu lệnh theo `question_type`; vạch mốc khi `pour_to_mark` |
| 3. Phần tử tương tác | 2–4 cốc theo `shape`, cột nước theo `fill_units` |
| 4. Lớp phản hồi | Hạt mừng khi đúng; rung hổ phách khi sai |

**Trạng thái thị giác riêng:** **vạch chia đơn vị** — khi gợi ý bật, thành cốc hiện các vạch ngang
bằng đúng `capacity_units`, để trẻ đếm thay vì ước lượng bằng mắt. Cấm — NEVER hiện vạch cùng lúc
với phản hồi đúng sai, vì khi đó nó thành lời giải.
**Thứ tự tuột khi thiết bị yếu:** gợn sóng mặt nước -> Lớp 4 (hạt). Hình dạng cốc và tỉ lệ cột nước
cấm — NEVER bỏ.

## 13. Ma trận seed mục tiêu

| Band | `compare` | `infer` | `verify` | Tổng mục tiêu |
|---|:--:|:--:|:--:|:--:|
| `5-6` | ≥4 | ≥3 | ≥3 | ≥10 |

Trục `what` mục tiêu: `capacity` · `quantity` · `size`.
Trục `theme` mục tiêu: ≥3 giá trị khác nhau trong 10 level của đợt đầu.

## 14. Ca sai không bắt được bằng schema

`conservation_trap` bật, hai cốc cùng `fill_units` là 3, khác `shape` — nhưng `capacity_units` cũng
khác nhau (4 và 12). Parse sạch và thoả `BR-E032-02`. Sai vì hai cột nước khi đó cao rất khác nhau
theo tỉ lệ, nên bẫy không còn là bẫy bảo toàn mà thành bài so tỉ lệ; trẻ trả lời "bằng nhau" sẽ
thấy hình phản bác chính mình. Cặp dựng bẫy phải cùng `fill_units` **và** cùng `capacity_units`.

Ca thứ hai: `question_type` là `same` trong khi bốn cốc đều khác `fill_units`. Parse sạch; đáp án
đúng là "không cốc nào" — một dạng câu hỏi mà bài không có ô để trả lời.

## 15. Trường trích từ registry

Chưa trích được — `packages/game-engine/src/templates/GT-032/template.ts` chưa tồn tại.
Bảng dưới là **giá trị đặt trước — `CHƯA ĐO`**, nguồn: plan #185 mục 2. Các cặp `limits` do người viết phiếu suy từ plan, **chưa có phép đo nào**.

**Luật giải quyết khi khuôn ra đời (`#205`): khuôn thắng.** Dev dựng `template.ts` theo nhu cầu thật của cơ chế, rồi **sửa phiếu theo** trong cùng PR. Cấm — NEVER bẻ khuôn cho khớp con số ở đây. Sổ số tạm: [`provisional-values.md`](../../../tasks/provisional-values.md).

| Trường | Giá trị đặt trước |
|---|---|
| `mechanic` | `pour-quantity` |
| `layouts` | `horizontal-row` · `split-columns` |
| `age_min` · `age_max` | 5 · 6 |
| `banned_age_bands` | `3-4` · `4-5` |
| `requires_tap_fallback` | `false` |
| `limits` | `item_count` [2, 4] · `distractor_count` [1, 3] · `target_count` [1, 1] |
| `asset_kinds` | `emoji` · `image` |
| `engine_session` | `GT032Session` |

## 16. Chiều sâu nội dung

Sáu số đo hiện tại và mục tiêu bậc 1 (`BR-ECD-01`…`-06`):
- `level_count`: hiện có 0, mục tiêu ≥6 — đợt đầu cấp 10 level `legacy_v1_ref` cộng 3 fixture
- `min_band_count`: hiện có 0, mục tiêu ≥1 — engine một band, sàn đạt bằng đúng band `5-6`
- `thinking_span`: hiện có 0, mục tiêu ≥2
- `what_span`: hiện có 0, mục tiêu ≥2
- `theme_span`: hiện có 0, mục tiêu ≥2
- `access_tier`: hiện có 0, mục tiêu ≥1 level `free` hoặc `login`
