---
sheet: GT-012
engine: flash-recall
batch: montessori
status: draft
reviewed: 2026-08-29
---

# GT-012 — Nhìn chớp rồi nhớ lại

## 1. Engine này dạy trẻ làm gì

Một nhóm vật hiện ra trong vài trăm mili giây rồi biến mất; trẻ chọn lại thứ vừa thấy. Engine
này luyện **subitizing** — nhận ra lượng nhỏ mà không cần đếm — và trí nhớ thị giác ngắn hạn.

Nó là engine duy nhất trong corpus có trục thời gian nằm trong độ khó chứ không nằm trong nội
dung.

## 2. Cơ chế và layout

Trích từ [`GT-012/template.ts:54`](../../../../packages/game-engine/src/templates/GT-012/template.ts).

| | |
|---|---|
| `mechanic` | `flash-recall` |
| `layouts` | `grid` · `horizontal-row` |
| `engine_session` | `FlashRecallSession` |
| `asset_kinds` | `emoji` · `image` · `audio` |
| `limits` | `item_count` [1, 6] · `distractor_count` [1, 5] · `target_count` [1, 1] |

## 3. Band tuổi và khả năng tiếp cận

| | |
|---|---|
| `age_min` · `age_max` | 3 · 6 |
| Band hợp lệ | `3-4` · `4-5` · `5-6` |
| `requires_tap_fallback` | Không |

## 4. Hình dạng `content_pack`

Khoá bậc một: `prompt` · `prompt_audio_ref` · `flash_items` · `arrangement` · `options`.

## 5. Trục độ khó

`flash_ms` · `item_count` · `distractor_count` · `allow_replay` · `hint_after_ms` · `allow_retry`.

`flash_ms` là trục quyết định. Dưới 400 mili giây thì kênh nhìn không kịp nạp ở lứa 3–4; trên
2000 thì trẻ đếm được và bài mất mục đích.

## 6. Ma trận seed mục tiêu

| Band | `recall` | `observe` | `count` | `compare` | Tổng mục tiêu |
|---|:--:|:--:|:--:|:--:|:--:|
| `3-4` | ≥2 | ≥2 | ≥1 | — | ≥5 |
| `4-5` | ≥2 | ≥1 | ≥2 | ≥1 | ≥6 |
| `5-6` | ≥2 | ≥1 | ≥2 | ≥1 | ≥6 |

Trục `what` mục tiêu: `quantity` · `number` · `colour` · `geometry`.
Hiện có 4 level, 2 giá trị `thinking`, 3 chủ đề — thủng bậc 1 về số lượng.

## 7. Ca sai không bắt được bằng schema

`flash_ms` = 600 với `item_count` = 6 ở band 3-4. Parse sạch, trong `limits`. Sai vì subitizing
ở lứa 3–4 dừng ở 3 vật; sáu vật trong 600 mili giây là bài không ai làm được, và thất bại lặp
lại dạy trẻ rằng mình kém.

## 8. Acceptance criteria riêng

```gherkin
Scenario: GT-012 — flash_items không hiện lại sau khi tắt
  Given flash_ms là 800 và allow_replay là false
  When thời gian flash kết thúc
  Then flash_items không còn trên màn hình
  And không thao tác nào của trẻ làm nó hiện lại

Scenario: GT-012 — allow_replay cho đúng một lượt xem lại
  Given allow_replay là true
  When trẻ dùng lượt xem lại
  Then không còn lượt xem lại thứ hai
```

## 9. Boundaries

**Always** — `item_count` ≤3 ở band 3-4; `flash_ms` trong khoảng 400–2000.

**Ask first** — `allow_replay` false ở band 3-4.

**Never** — `flash_ms` dưới 400; `item_count` 6 ở band 3-4.

## 10. Câu hỏi còn mở

| # | Câu hỏi | Chặn gì | Chặn phase | Chủ |
|---|---|---|---|---|
| 1 | `flash_ms` có nên phụ thuộc band tuổi ở tầng contract thay vì để người soạn tự đặt? `limits` hiện không ràng buộc theo band | Ràng buộc `flash_ms` theo band | P5 | Backend |

## 11. Hợp đồng vẽ

Hợp đồng chung, gồm bốn lớp và năm trạng thái thị giác:
[`engine-render-contract.md`](../engine-render-contract.md).

**Slot dùng:** `grid` hoặc `horizontal-row`.

| Lớp | Engine này vẽ gì |
|---|---|
| 1. Nền cảnh | Nền phẳng, cộng vòng đếm ngược thời gian chớp |
| 2. Phần tử tĩnh | Không có phần tử tĩnh trong pha chớp |
| 3. Phần tử tương tác | Pha chớp: `flash_items` hiện đủ `flash_ms`. Pha trả lời: dải `options` chạm được |
| 4. Lớp phản hồi | Hạt khi đúng; nếu `allow_replay` còn lượt thì nút xem lại nhấn được |

Trạng thái riêng: **pha chớp** và **pha trả lời** là hai màn hình khác nhau. Cấm — NEVER vẽ `options` trong pha chớp, và cấm vẽ `flash_items` trong pha trả lời.

**Thứ tự tuột khi thiết bị yếu:** theo mục 7.4 của hợp đồng chung. Lớp 3 và vùng chạm của nó
cấm — NEVER bỏ.
