---
spec: ENGINE-GT015
title: Lưới không lặp — sudoku-mini
area: platform
status: approved
mvp: true
phase: P4
reviewed: 2026-08-30
engine: sudoku-mini
batch: mvp
owns:
  - Hợp đồng nội dung của engine GT-015
  - Hợp đồng vẽ của engine GT-015
  - Ma trận seed mục tiêu của engine GT-015
depends_on:
  - GAME-TEMPLATE-CONTRACT
  - GAME-LAYOUT-ENGINE
  - ENGINE-RENDER-CONTRACT
  - ENGINE-SPEC-SHEET
---

# GT-015 — Lưới không lặp

## 1. Objective

Trẻ điền ký hiệu vào lưới 2x2 hoặc 3x3 sao cho không hàng nào, không cột nào lặp ký hiệu.
Engine này dạy **ràng buộc**: một ô không quyết định riêng lẻ mà bị chặn bởi hàng và cột của
nó. Đó là bước đầu của suy luận có điều kiện.

## 2. Actors

| Actor | Quyền cần | Làm được gì ở đây |
|---|---|---|
| Trẻ 5–6 | — | Tương tác với engine sudoku-mini |
| Người soạn nội dung | `content_reviewer` | Soạn `content_pack` và `difficulty_params` |
| Cổng | — | Kiểm tra tính hợp lệ |

## 3. Entry points

| Route / màn hình | Actor | Ghi chú |
|---|---|---|
| `packages/game-engine/src/templates/GT-015/template.ts` | Dev | Định nghĩa contract |
| `packages/game-engine/src/templates/GT-015/session.ts` | Dev | Logic + render |
| `docs/specs/01-platform/engines/GT-015.md` | Dev | Spec này |

## 4. Main flow

1. Vòng lặp nạp `content_pack` đã parse bằng schema.
2. `setupEntities()` khởi tạo trạng thái.
3. Trẻ tương tác theo cơ chế `sudoku-mini`.
4. `validateAction` kiểm tra hành động.
5. Phản hồi thị giác theo trạng thái.
6. `checkWinCondition()` trả `true` khi hoàn thành — thuần, không side effect.

## 5. Alternative flows

| Nhánh | Điều kiện | Hành vi |
|---|---|---|
| Sai | Trẻ thao tác sai | Phản hồi amber nudge, phiên vẫn chạy |
| Hết giờ gợi ý | Timeout | Scaffolding highlight |
| Asset hỏng | Emoji/image fail | Vẽ ô giữ chỗ |
| Thiết bị yếu | FPS tụt | Bỏ hạt lớp 4, giữ lớp 3 |

## 6. Business rules

| ID | Rule | Vì sao |
|---|---|---|
| `BR-E015-01` | Band hợp lệ: `3-4`, `4-5`, `5-6` | Phù hợp phát triển vận động và nhận thức của lứa tuổi |
| `BR-E015-02` | `checkWinCondition()` thuần — không side effect | Tính toán không được ảnh hưởng trạng thái |

## 7. Data

Contract đầy đủ nằm ở schema trong `template.ts` — cấm chép lại schema (`BR-ESS-03`).

## 8. API contract

Không có. Engine chạy trong tiến trình frontend/canvas runtime.

## 9. Acceptance criteria

```gherkin
Scenario: GT-015 — lưới có đúng một lời giải
  Given một content_pack với grid_size 3 và 4 ô trống
  When bộ giải liệt kê mọi lời giải
  Then đếm được đúng một lời giải

Scenario: GT-015 — ô đã điền sẵn không sửa được
  Given một ô thuộc cells cho trước
  When trẻ chạm vào ô đó
  Then giá trị không đổi

Scenario: BR-E015-01 — band hợp lệ
  Given một level GT-015
  When band nằm ngoài phạm vi cho phép
  Then cổng validate đỏ

Scenario: BR-E015-02 — checkWinCondition thuần
  Given một phiên GT-015
  When gọi checkWinCondition 100 lần
  Then trạng thái không đổi
```

## 10. Boundaries

**Always** — kiểm lời giải duy nhất bằng bộ giải trước khi seed; ký hiệu phân biệt được bằng hình, không chỉ bằng màu.

**Ask first** — `grid_size` 3 với `blank_count` > 3.

**Never** — lưới nhiều lời giải; dùng engine này dưới band 5-6.

## 11. Open questions

| # | Câu hỏi | Chặn gì | Chặn phase | Chủ |
|---|---|---|---|---|
| 1 | Bộ giải kiểm lời giải duy nhất viết một lần dùng chung với `GT-013` hay mỗi engine một bộ? Cả hai đều cần kiểm tồn tại và duy nhất | Thi công bộ sinh | P4 | Backend |

## 12. Hợp đồng vẽ

Hợp đồng chung, gồm bốn lớp và năm trạng thái thị giác:
[`engine-render-contract.md`](../engine-render-contract.md).

**Slot dùng:** `matrix-slot-grid` hoặc `grid`; ô trống là slot `target`.

| Lớp | Engine này vẽ gì |
|---|---|
| 1. Nền cảnh | Lưới cùng đường kẻ vùng `regions` nếu có |
| 2. Phần tử tĩnh | Ô cho trước, không sửa được, vẽ khác ô trống |
| 3. Phần tử tương tác | Ô trống cộng dải ký hiệu chạm được |
| 4. Lớp phản hồi | Ô sáng khi điền đúng; hàng hoặc cột hoàn thành nhấp nháy nhẹ |

Trạng thái riêng: **ô cho trước** phải phân biệt rõ với ô trẻ đã điền, bằng nền chứ không bằng màu chữ.

**Thứ tự tuột khi thiết bị yếu:** theo mục 7.4 của hợp đồng chung. Lớp 3 và vùng chạm của nó
cấm — NEVER bỏ.

## 13. Ma trận seed mục tiêu

| Band | `infer` | `plan` | `observe` | `sort` | Tổng mục tiêu |
|---|:--:|:--:|:--:|:--:|:--:|
| `5-6` | ≥4 | ≥3 | ≥3 | ≥2 | ≥12 |

Trục `what` mục tiêu: `pattern` · `colour` · `geometry` · `number`.
Hiện có 3 level, 1 giá trị `thinking`, 3 chủ đề — mức mẫu, thủng bậc 1.

## 14. Ca sai không bắt được bằng schema

Lưới 3x3 với 4 ô trống có **hai** lời giải khác nhau. Parse sạch; sai vì trẻ điền một lời
giải hợp lệ và bị chấm sai. Mỗi lưới phải có đúng một lời giải, và điều đó phải kiểm bằng bộ
giải trước khi seed.

Ca thứ hai: bốn ký hiệu quá giống nhau ở lứa 5–6 (bốn sắc thái xanh). Bài trở thành bài phân
biệt màu chứ không phải bài ràng buộc.

## 15. Trường trích từ registry

Trích từ [`GT-015/template.ts`](../../../../packages/game-engine/src/templates/GT-015/template.ts).

| Trường | Giá trị |
|---|---|
| `mechanic` | `sudoku-mini` |
| `layouts` | `matrix-slot-grid` · `grid` |
| `age_min` · `age_max` | 5 · 6 |
| `banned_age_bands` | — |
| `requires_tap_fallback` | `true` |
| `asset_kinds` | `emoji` · `image` · `audio` |
| `engine_session` | `SudokuMiniSession` |

## 16. Chiều sâu nội dung

- `level_count`: cần đo, mục tiêu ≥6
- `min_band_count`: cần đo, mục tiêu ≥1
- `thinking_span`: cần đo, mục tiêu ≥2
- `what_span`: cần đo, mục tiêu ≥2
- `theme_span`: cần đo, mục tiêu ≥2
- `access_tier`: cần đo, ≥1 level `free` hoặc `login`
