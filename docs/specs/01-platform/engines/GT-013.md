---
spec: ENGINE-GT013
title: Tìm đường mê cung — maze-route
area: platform
status: approved
mvp: true
phase: P4
reviewed: 2026-08-30
engine: maze-route
batch: montessori
owns:
  - Hợp đồng nội dung của engine GT-013
  - Hợp đồng vẽ của engine GT-013
  - Ma trận seed mục tiêu của engine GT-013
depends_on:
  - GAME-TEMPLATE-CONTRACT
  - GAME-LAYOUT-ENGINE
  - ENGINE-RENDER-CONTRACT
  - ENGINE-SPEC-SHEET
---

# GT-013 — Tìm đường mê cung

## 1. Objective

Trẻ vạch một đường từ điểm đầu tới đích qua lưới, đi qua các ô bắt buộc và tránh ngõ cụt.
Engine này là nơi rõ nhất trong corpus để dạy **lập kế hoạch trước khi hành động**: nhìn
trước vài bước rồi mới chạm.

## 2. Actors

| Actor | Quyền cần | Làm được gì ở đây |
|---|---|---|
| Trẻ 4–6 | — | Tương tác với engine maze-route |
| Người soạn nội dung | `content_reviewer` | Soạn `content_pack` và `difficulty_params` |
| Cổng | — | Kiểm tra tính hợp lệ |

## 3. Entry points

| Route / màn hình | Actor | Ghi chú |
|---|---|---|
| `packages/game-engine/src/templates/GT-013/template.ts` | Dev | Định nghĩa contract |
| `packages/game-engine/src/templates/GT-013/session.ts` | Dev | Logic + render |
| `docs/specs/01-platform/engines/GT-013.md` | Dev | Spec này |

## 4. Main flow

1. Vòng lặp nạp `content_pack` đã parse bằng schema.
2. `setupEntities()` khởi tạo trạng thái.
3. Trẻ tương tác theo cơ chế `maze-route`.
4. `validateAction` kiểm tra hành động.
5. Phản hồi thị giác theo trạng thái.
6. `checkWinCondition()` trả `true` khi hoàn thành — thuần, không side effect.

## 5. Alternative flows

| Nhánh | Điều kiện | Hành vi |
|---|---|---|
| Sai | Trẻ thao tác sai | Phản hồi amber nudge, phiên vẫn chạy |
| Hết giờ gợi ý | Timeout | Scaffolding highlight |
| Asset hỏng | Emoji/image fail | Vẽ ô giữ chỗ |
| Thiết bị yếu | FPS tụt | Bỏ hạt lớp 4, giữ lớp 3 |

## 6. Business rules

| ID | Rule | Vì sao |
|---|---|---|
| `BR-E013-01` | Band hợp lệ: `3-4`, `4-5`, `5-6` | Phù hợp phát triển vận động và nhận thức của lứa tuổi |
| `BR-E013-02` | `checkWinCondition()` thuần — không side effect | Tính toán không được ảnh hưởng trạng thái |

## 7. Data

Contract đầy đủ nằm ở schema trong `template.ts` — cấm chép lại schema (`BR-ESS-03`).

## 8. API contract

Không có. Engine chạy trong tiến trình frontend/canvas runtime.

## 9. Acceptance criteria

```gherkin
Scenario: GT-013 — tồn tại ít nhất một đường hợp lệ
  Given một content_pack với grid và required_cells
  When bộ giải chạy trên lưới
  Then có ít nhất một đường từ đầu tới đích đi qua mọi required_cell

Scenario: GT-013 — đi vào ngõ cụt lùi lại được
  Given trẻ đã đi vào một dead end
  When trẻ chạm ô liền trước
  Then đường đi lùi một bước
  And không tính là kết thúc phiên

Scenario: BR-E013-01 — band hợp lệ
  Given một level GT-013
  When band nằm ngoài phạm vi cho phép
  Then cổng validate đỏ

Scenario: BR-E013-02 — checkWinCondition thuần
  Given một phiên GT-013
  When gọi checkWinCondition 100 lần
  Then trạng thái không đổi
```

## 10. Boundaries

**Always** — kiểm tồn tại đường hợp lệ bằng bộ giải trước khi seed; cho lùi bước.

**Ask first** — `dead_end_count` > 3 ở band 4-5; `input_mode` vuốt liên tục ở band 4-5.

**Never** — lưới không có đường hợp lệ; hai đường tới đích mà chỉ một được chấm đúng.

## 11. Open questions

| # | Câu hỏi | Chặn gì | Chặn phase | Chủ |
|---|---|---|---|---|
| 1 | Engine này cần bộ giải để bảo đảm có lời giải. Bộ sinh level ở [`level-generator-kit.md`](../level-generator-kit.md) có sinh được lưới không, hay engine này thuộc nhóm soạn tay? Trùng câu hỏi 2 ở mục 11 của file đó | Phạm vi lô bộ sinh đầu tiên | P4 | Backend |

## 12. Hợp đồng vẽ

Hợp đồng chung, gồm bốn lớp và năm trạng thái thị giác:
[`engine-render-contract.md`](../engine-render-contract.md).

**Slot dùng:** `grid`; mỗi ô lưới là một slot `neutral`.

| Lớp | Engine này vẽ gì |
|---|---|
| 1. Nền cảnh | Lưới mê cung: tường, ô đi được, ô bắt buộc, mốc đầu và đích |
| 2. Phần tử tĩnh | Ô bắt buộc đánh dấu riêng, luôn hiện |
| 3. Phần tử tương tác | Đường đi trẻ vạch, vẽ tích luỹ theo ô đã qua |
| 4. Lớp phản hồi | Đường đi sáng khi tới đích qua đủ ô bắt buộc; ngõ cụt rung nhẹ |

Trạng thái riêng: **ô đã đi qua** và **ô lùi lại**. Lùi phải xoá đúng một ô, cấm xoá cả đường — đó là điều kiện để `allow_retry` có nghĩa.

**Thứ tự tuột khi thiết bị yếu:** theo mục 7.4 của hợp đồng chung. Lớp 3 và vùng chạm của nó
cấm — NEVER bỏ.

## 13. Ma trận seed mục tiêu

| Band | `plan` | `predict` | `observe` | `sequence` | Tổng mục tiêu |
|---|:--:|:--:|:--:|:--:|:--:|
| `4-5` | ≥2 | ≥1 | ≥2 | ≥1 | ≥6 |
| `5-6` | ≥3 | ≥2 | ≥1 | ≥1 | ≥7 |

Trục `what` mục tiêu: `space` · `pattern` · `category`.
Hiện có 3 level, 1 giá trị `thinking`, 3 chủ đề — mức mẫu, thủng bậc 1.

Engine này là nguồn chính của giá trị `plan`, đang ở 3 trên toàn catalog và dưới sàn 5 của
[`thinking-coverage-matrix.md`](../../08-quality/thinking-coverage-matrix.md).

## 14. Ca sai không bắt được bằng schema

Lưới có hai đường đi hợp lệ tới đích, và chỉ một đường đi qua đủ `required_cells`. Parse sạch;
sai vì trẻ tìm ra đường ngắn hơn, tới đích, và bị chấm sai. Hoặc lưới chỉ có một đường, hoặc
`prompt` phải nói rõ điều kiện đi qua.

## 15. Trường trích từ registry

Trích từ [`GT-013/template.ts`](../../../../packages/game-engine/src/templates/GT-013/template.ts).

| Trường | Giá trị |
|---|---|
| `mechanic` | `maze-route` |
| `layouts` | `grid` |
| `age_min` · `age_max` | 4 · 6 |
| `banned_age_bands` | — |
| `requires_tap_fallback` | `true` |
| `asset_kinds` | `audio` |
| `engine_session` | `MazeRouteSession` |

## 16. Chiều sâu nội dung

- `level_count`: cần đo, mục tiêu ≥6
- `min_band_count`: cần đo, mục tiêu ≥1
- `thinking_span`: cần đo, mục tiêu ≥2
- `what_span`: cần đo, mục tiêu ≥2
- `theme_span`: cần đo, mục tiêu ≥2
- `access_tier`: cần đo, ≥1 level `free` hoặc `login`
