{
  "name": "@mindkid/monorepo",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24",
    "pnpm": ">=11"
  },
  "packageManager": "pnpm@11.16.0",
  "scripts": {
    "prepare": "lefthook install",
    "dev": "pnpm --filter @mindkid/web dev",
    "dev:admin": "pnpm --filter @mindkid/admin dev",
    "dev:worker": "pnpm --filter @mindkid/worker dev",
    "dev:all": "pnpm --parallel --filter './apps/*' dev",
    "build": "pnpm --recursive --if-present run build",
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "format": "biome format --write .",
    "lint:deps": "depcruise apps packages --config .dependency-cruiser.cjs",
    "typecheck": "pnpm typecheck:root",
    "typecheck:root": "tsc --noEmit -p tsconfig.json",
    "typecheck:web": "pnpm --filter @mindkid/web exec vue-tsc --noEmit -p .nuxt/tsconfig.app.json",
    "typecheck:web:server": "pnpm --filter @mindkid/web exec vue-tsc --noEmit -p .nuxt/tsconfig.server.json",
    "typecheck:admin": "pnpm --filter @mindkid/admin exec vue-tsc --noEmit -p .nuxt/tsconfig.app.json",
    "test": "NODE_OPTIONS=--max-old-space-size=4096 vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "test:deploy": "bash infra/scripts/tests/run.sh",
    "check": "pnpm lint && pnpm lint:deps && pnpm typecheck",
    "db:generate": "pnpm --filter @mindkid/db run generate",
    "db:migrate": "pnpm --filter @mindkid/db run migrate",
    "db:seed": "pnpm --filter @mindkid/db run seed",
    "db:restore": "pnpm --filter @mindkid/worker run restore",
    "services": "tsx scripts/check-services.ts",
    "deploy": "tsx scripts/deploy/cli.ts"
  },
  "devDependencies": {
    "@biomejs/biome": "catalog:",
    "@types/node": "catalog:",
    "dependency-cruiser": "catalog:",
    "drizzle-orm": "catalog:",
    "ioredis": "catalog:",
    "lefthook": "^2.1.10",
    "postgres": "catalog:",
    "typescript": "catalog:",
    "ultracite": "catalog:",
    "vitest": "catalog:"
  }
}
