{
  "name": "@mindkid/monorepo",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24",
    "pnpm": ">=11"
  },
  "packageManager": "pnpm@11.16.0",
  "scripts": {
    "prepare": "lefthook install",
    "dev": "pnpm --filter @mindkid/web dev",
    "dev:admin": "pnpm --filter @mindkid/admin dev",
    "dev:worker": "pnpm --filter @mindkid/worker dev",
    "dev:all": "pnpm --parallel --filter './apps/*' dev",
    "build": "pnpm --recursive --if-present run build",
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "lint:deps": "depcruise apps packages scripts --config .dependency-cruiser.cjs",
    "typecheck:root": "tsc --noEmit",
    "typecheck:apps": "pnpm --recursive --filter \"./apps/*\" run typecheck",
    "typecheck": "tsc --noEmit && pnpm --recursive --filter \"./apps/*\" run typecheck",
    "typecheck:gate": "node scripts/typecheck/typecheck-gate.ts",
    "test": "NODE_OPTIONS=--max-old-space-size=4096 vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "test:deploy": "bash infra/scripts/tests/run.sh",
    "check": "pnpm lint && pnpm lint:deps && pnpm typecheck && pnpm test && pnpm test:deploy",
    "db:generate": "pnpm --filter @mindkid/db run generate",
    "db:migrate": "pnpm --filter @mindkid/db run migrate",
    "db:seed": "pnpm --filter @mindkid/db run seed",
    "db:restore": "pnpm --filter @mindkid/worker run restore",
    "services": "tsx scripts/check-services.ts",
    "deploy": "tsx scripts/deploy/cli.ts"
  },
  "devDependencies": {
    "@biomejs/biome": "catalog:",
    "@mindkid/config": "workspace:*",
    "@types/node": "catalog:",
    "dependency-cruiser": "catalog:",
    "drizzle-orm": "catalog:",
    "ioredis": "catalog:",
    "lefthook": "catalog:",
    "postgres": "catalog:",
    "tsx": "catalog:",
    "typescript": "catalog:",
    "ultracite": "catalog:",
    "vitest": "catalog:"
  }
}
